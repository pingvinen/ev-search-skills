---
phase: 02-detail-skill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/ev-detail/SKILL.md
autonomous: true
requirements:
  - DETL-01
  - DETL-02
  - DETL-03
  - DETL-04
  - DETL-05
  - DETL-06
  - DETL-07
  - DETL-08
  - DETL-09
  - DETL-10
  - TIRE-01
  - OWNR-01
  - SRCH-07
user_setup: []

must_haves:
  truths:
    - "Running /ev-detail with a car name resolves it to an ev-database.org variant and fetches deep specs live (DETL-01)"
    - "The skill writes projects/<active>/research/<make-model>.md conforming to car-template.md (DETL-02)"
    - "All spec figures (WLTP range, charging, battery, performance, cargo, DKK price) are present and live-sourced (DETL-03, DETL-04)"
    - "Every populated fact traces to a source URL + fetch date in the Sources table (DETL-05)"
    - "FDM real-world 110 km/h range, verdict, and pros/cons are captured when an FDM article exists, and a gap note is written when it does not (DETL-06, DETL-07, DETL-08)"
    - "Tier 1 and tier 2 DKK prices appear for purchase_type new; used and leasing branches capture market/monthly ranges instead (DETL-09, SRCH-07)"
    - "Platform type (dedicated EV vs adapted ICE) is recorded (DETL-10)"
    - "Tire size (front, and rear if different) is captured from wheel-size.com (TIRE-01)"
    - "FDM reliability reputation is captured with a confidence label and source (OWNR-01)"
    - "Multi-variant models auto-select per BRIEF with rationale (D-05/D-06); a pre-existing file triggers an overwrite/skip prompt (D-09)"
  artifacts:
    - path: ".claude/skills/ev-detail/SKILL.md"
      provides: "The /ev-detail skill: step-numbered procedure from name resolution through file write and state update"
      min_lines: 120
      contains: "name: ev-detail"
  key_links:
    - from: ".claude/skills/ev-detail/SKILL.md"
      to: "state.md"
      via: "backtick injection !`cat state.md` resolves the active project at invocation"
      pattern: "cat state.md"
    - from: ".claude/skills/ev-detail/SKILL.md"
      to: "car-template.md"
      via: "output file conforms to the car-template.md section/row contract"
      pattern: "car-template"
    - from: ".claude/skills/ev-detail/SKILL.md"
      to: "ev-database.org"
      via: "WebFetch of the selected /car/{ID}/{Make-Model} page for mandatory specs"
      pattern: "ev-database.org"
---

<objective>
Author the core `/ev-detail` skill at `.claude/skills/ev-detail/SKILL.md`: a self-contained, step-numbered procedure that resolves a free-text car name to an ev-database.org variant, fetches deep specs from multiple live sources, branches on the project's purchase_type, and writes a fully-sourced per-car research file conforming to `car-template.md`, then updates the per-project state.md.

Purpose: This is the highest-value piece of the suite (ROADMAP Phase 2 goal). It turns "what is this EV like?" into a comparable, sourced research file. It implements all 10 detail requirements plus tire size (TIRE-01), FDM reliability reputation (OWNR-01), and purchase-type-aware pricing (SRCH-07).
Output: `.claude/skills/ev-detail/SKILL.md` (one self-contained skill file; no supporting files per 02-RESEARCH.md).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@car-template.md
@.claude/skills/ev-new-project/SKILL.md
@.claude/skills/ev-switch-project/SKILL.md
@.planning/phases/02-detail-skill/02-CONTEXT.md
@.planning/phases/02-detail-skill/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skill frontmatter, invocation injection, name resolution, variant discovery and selection, and the mandatory ev-database.org spec fetch</name>
  <files>.claude/skills/ev-detail/SKILL.md</files>
  <read_first>
    - .claude/skills/ev-new-project/SKILL.md (frontmatter shape, !`cat state.md` backtick injection, $ARGUMENTS, step-numbered body, overwrite-guard idiom)
    - .claude/skills/ev-switch-project/SKILL.md (active-project read pattern; how the active project is resolved from global state.md)
    - .planning/phases/02-detail-skill/02-RESEARCH.md (§ Open Technical Questions Resolved #1 variant discovery + URL pattern; #2 spec field map; § Common Pitfalls 1, 2, 4, 5, 6; § Code Examples for frontmatter/injection/variant/re-run)
    - .planning/phases/02-detail-skill/02-CONTEXT.md (D-03 ev-database mandatory-or-abort; D-05/D-06 variant selection + middle-tier tie-breaker; D-09 re-run guard; D-10 sourcing rigor)
    - car-template.md (the exact Specs rows and EV platform field this step's fetched values populate)
    - state.md (repo root — the active_project field the skill reads)
  </read_first>
  <action>
    Create `.claude/skills/ev-detail/SKILL.md`. Write the frontmatter exactly per 02-RESEARCH.md § Code Examples: `name: ev-detail`; a `description` that triggers on "research a car / fetch specs for a model / add a car to the project"; `allowed-tools: WebFetch, WebSearch, Read, Write, Bash(ls *)`; `context: fork`; `agent: Explore`; `argument-hint: [car-model]`. Do NOT set `disable-model-invocation` — this skill is user-invoked and may be auto-triggered by description. Mirror the conventions in the two existing skills.

    After the frontmatter, add the invocation-time backtick injection block: inject the global state via `!`cat state.md`` with a fallback message instructing the user to run `/ev-new-project` first if no active project; then `Car to research: $ARGUMENTS`.

    Then write the first half of the step-numbered body:

    Step 1 — Resolve active project: read the injected global state to get `active_project`. If none, stop and tell the user to create/switch a project first.

    Step 2 — Read project inputs: Read `projects/<active>/BRIEF.md` (budget, range, must-haves, `purchase_type`) and `projects/<active>/state.md` (existing Research Progress). These drive variant selection (D-05) and the later purchase-type branch (D-07).

    Step 3 — Re-run detection (D-09): derive the normalized filename from `$ARGUMENTS` using the rules in 02-RESEARCH.md § #9 (lowercase, spaces→hyphens, strip parens/slashes/dots, keep battery variant only if the user typed it). Run `Bash(ls projects/<active>/research/<filename>.md 2>/dev/null)`. If the file exists, pause and ask the user to reply "overwrite" or "skip"; wait for the response; on "skip" exit without changes. (Support overwrite/skip only; update-in-place is out of scope per assumption A1.)

    Step 4 — Discover variants (WebSearch): run `WebSearch: "ev-database.org <make> <model> specifications"`. Extract every result URL matching `https://ev-database.org/[uk/]car/{ID}/{Make-Model-Variant}`. The variant/battery is encoded in the slug — do NOT fetch each variant page (token pitfall #2). Prefer `/uk/` URLs (pitfall #4); spec values are identical across regions but pricing on the page is not the DKK source.

    Step 5 — Select the variant (D-05/D-06): reason over the slug-encoded battery/trim against the BRIEF budget and minimum range. Apply the middle-tier tie-breaker when ambiguous (3 variants → pick the middle by battery/price; 2 variants → larger battery if budget allows). State the selection in the output: selected variant + ID, reason, and the other variants considered.

    Step 6 — Fetch specs (DETL-01, mandatory, D-03): WebFetch only the selected variant's ev-database.org page. Extract the fields in 02-RESEARCH.md § #2 into the car-template Specs rows: WLTP range, real-world range (mild) and (cold), usable battery kWh, DC peak kW (rated), AC kW, 10-80% DC time, 0-100 km/h (convert from 0-62 mph), cargo L, tow capacity kg, power kW, and the EV platform field (DETL-10 — "Dedicated EV platform (<name>)" or "Adapted ICE platform (<name>)" from the "EV Dedicated Platform" label). If the car cannot be found on ev-database.org, ABORT with a clear message and write NO file (D-03). Record this fetch's URL + today's date for the Sources table (DETL-05).

    The skill prompt must explicitly forbid using training data for any spec value (DETL-04) and forbid conflating the rated DC kW with any later FDM-measured figure (pitfall #5) and forbid averaging WLTP with real-world range (pitfall #6).

    Do NOT inline car-template.md's full contents into the skill — instruct the skill to follow `car-template.md` as the output contract by reference.
  </action>
  <verify>
    <automated>test -f .claude/skills/ev-detail/SKILL.md && grep -q 'name: ev-detail' .claude/skills/ev-detail/SKILL.md && grep -q 'context: fork' .claude/skills/ev-detail/SKILL.md && grep -q 'agent: Explore' .claude/skills/ev-detail/SKILL.md && grep -q 'cat state.md' .claude/skills/ev-detail/SKILL.md && grep -qi 'ev-database.org' .claude/skills/ev-detail/SKILL.md && grep -qi 'training data' .claude/skills/ev-detail/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md exists with frontmatter matching 02-RESEARCH.md (name, description, allowed-tools including WebFetch/WebSearch/Read/Write/Bash(ls *), context: fork, agent: Explore, argument-hint), and NO disable-model-invocation.
    - Invocation block injects global state via `!`cat state.md`` and binds `$ARGUMENTS` to the car name.
    - Steps 1-6 are present and ordered: resolve active project → read BRIEF/state → re-run guard (overwrite/skip) → WebSearch variant discovery → variant selection with stated rationale and middle-tier tie-breaker → mandatory ev-database.org WebFetch of one variant.
    - The skill instructs abort-without-file when the car is absent from ev-database.org (D-03).
    - The skill carries explicit "no training data for specs" (DETL-04), "record rated vs FDM-measured DC kW separately" (pitfall #5), and "never average WLTP and real-world range" (pitfall #6) instructions.
    - The skill references car-template.md as the output contract rather than inlining it.
  </acceptance_criteria>
  <done>The skill's front half exists: it resolves the project, guards re-runs, discovers and selects an ev-database.org variant per BRIEF, and fetches mandatory live specs with sourcing discipline.</done>
</task>

<task type="auto">
  <name>Task 2: FDM best-effort fetch, tire size, purchase-type pricing branch, file write, and per-project state update</name>
  <files>.claude/skills/ev-detail/SKILL.md</files>
  <read_first>
    - car-template.md (FDM Test Notes, Tire Research, Ownership Signals, Danish Market Context, Pros, Cons, Sources sections this step populates)
    - .planning/phases/02-detail-skill/02-RESEARCH.md (§ #3 FDM discovery 2-attempt limit + extractable fields; § #4 wheel-size.com URL pattern + front/rear rule; § #5 Bilbasen Blog used market range; § #6 leasing range + no-residual note; § #10 per-project state.md update format; § Common Pitfalls 3 greengarage 404)
    - .planning/phases/02-detail-skill/02-CONTEXT.md (D-04 best-effort sources + graceful gap notes; D-07 purchase-type branch; D-08 ranges not listings; D-10 confidence labels)
    - .claude/skills/ev-new-project/SKILL.md (per-project state.md Research Progress table columns to append to)
    - .planning/phases/02-detail-skill/02-02-PLAN.md (Task 1 wrote Steps 1-6 into the same SKILL.md; this task continues the same file from Step 7)
  </read_first>
  <action>
    Continue the SAME `.claude/skills/ev-detail/SKILL.md` file, appending the second-half steps (do not rewrite Task 1's steps).

    Step 7 — FDM test (best-effort, DETL-06/07/08, OWNR-01): attempt FDM discovery with at most two WebSearch queries per 02-RESEARCH.md § #3 — attempt 1 `site:fdm.dk/tests biltest <make> <model>`, attempt 2 (if empty) `fdm.dk <make> <model> elbil test`. If a URL is found, WebFetch the most recent article and extract: measured range at 110 km/h (the real-world range figure — write it into the Real-world range row and source it to fdm.dk, DETL-06), measured range at 0°C, FDM-measured DC peak kW (record in FDM Test Notes ONLY, labeled measured, never overwriting the rated spec — pitfall #5), verdict, Styrker, Svagheder (DETL-07), DKK prices, and publication date. Capture the reliability reputation from the FDM narrative into Ownership Signals labeled `HIGH (FDM test, <article date>)` (OWNR-01, D-10). If both searches return nothing, write "No FDM test found as of <today>" in FDM Test Notes (DETL-08) and continue — never abort for a missing best-effort source (D-04).

    Step 8 — Tire size (TIRE-01, best-effort): WebFetch `https://www.wheel-size.com/size/<make-lower>/<model-lower>/<year>/` using the model year from the ev-database page. Extract OEM front tire size and rear size; if front == rear write the same value in both Specs rows with a "(square setup)" note; only fill Tire size (rear) when it differs. If the page 404s, write "Tire size: unconfirmed — check manufacturer spec sheet" (do not abort). Source the tire rows to the wheel-size.com URL + today's date.

    Step 9 — Purchase-type pricing branch (SRCH-07, D-07, D-08): read `purchase_type` from BRIEF.md and branch:
      - `new` → capture tier 1 ("from") and tier 2 (best-value) DKK prices from the manufacturer's Danish website (DETL-09); populate the two Price DK rows. ev-database.org page pricing is NOT the DKK source (pitfall #4).
      - `used` → WebSearch `"bilbasen <make> <model> brugt pris"`, WebFetch the Bilbasen Blog article (not the listing page — pitfall/anti-pattern), and capture a low / typical / high DKK market range with the article's publication date; phrase it as a market range, not a single listing (D-08).
      - `leasing` → WebSearch `"privatleasing <make> <model> månedlig DKK"`, WebFetch an editorial article (Bilbasen Blog or fdm.dk/nyheder), capture a typical monthly DKK range + upfront, and note that Danish privatleasing publishes no residual figure (record depreciation-risk context instead, per 02-RESEARCH.md § #6). Do NOT invent a residual number.
    Every captured price gets a Sources row with URL + fetch/article date.

    Step 10 — greengarage.dk: do NOT instruct a mandatory fetch. greengarage.dk model pages 404 (pitfall #3, § State of the Art). At most an optional silent probe; a 404 is NOT logged as a gap.

    Step 11 — Write the file (DETL-02, DETL-05): write `projects/<active>/research/<filename>.md` conforming to car-template.md section-by-section. Fill the Quick Verdict (does it meet the BRIEF), all populated Specs rows, EV platform, FDM Test Notes, Tire Research (size only this phase — D-01; no pricing/scoring/recommendations), Ownership Signals, Danish Market Context (leave tax/insurance for Phase 4 — note "captured in Phase 4 / Danish enrichment" rather than inventing), Pros, Cons. Then build the Sources table so that every populated fact above has a row with its source URL and fetch date (DETL-05); the skill must assert it left no populated spec cell unsourced.

    Step 12 — Update per-project state (per 02-RESEARCH.md § #10): append one row to the `projects/<active>/state.md` Research Progress table — `| <variant name> | research/<filename>.md | <today> | yes (<fdm date>) / no / partial |` — and add any fetch-reliability observations to Source Reliability Notes.

    Step 13 — Confirm to the user: report the file path, the selected variant + reason, FDM found/not-found, and any gaps (unsourced or unconfirmed fields).

    Keep the skill self-contained (no supporting files). Do NOT add tire pricing, tire scoring, or top-3 tire recommendations — those are deferred to Phase 5 (D-01/D-02); this phase captures tire SIZE only.
  </action>
  <verify>
    <automated>grep -qi 'fdm' .claude/skills/ev-detail/SKILL.md && grep -qi 'No FDM test found' .claude/skills/ev-detail/SKILL.md && grep -qi 'wheel-size.com' .claude/skills/ev-detail/SKILL.md && grep -qi 'purchase_type' .claude/skills/ev-detail/SKILL.md && grep -qi 'bilbasen' .claude/skills/ev-detail/SKILL.md && grep -qi 'leasing' .claude/skills/ev-detail/SKILL.md && grep -qi 'Research Progress' .claude/skills/ev-detail/SKILL.md && grep -qi 'Sources' .claude/skills/ev-detail/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - The skill performs FDM discovery with a 2-attempt cap, extracts measured 110 km/h range → Real-world range row sourced to fdm.dk (DETL-06), captures verdict + Styrker + Svagheder (DETL-07), and writes "No FDM test found as of <date>" when absent (DETL-08); FDM reputation is confidence-labeled (OWNR-01).
    - FDM-measured DC kW is recorded separately in FDM Test Notes and never overwrites the rated spec (pitfall #5).
    - Tire size is fetched from wheel-size.com with the front/rear rule and a graceful unconfirmed note (TIRE-01).
    - The purchase_type branch produces: tier1+tier2 DKK for new (DETL-09); low/typical/high market range for used; monthly range + no-residual note for leasing (SRCH-07, D-07, D-08).
    - greengarage.dk is not a mandatory source and a 404 is not logged as a gap (pitfall #3).
    - The skill writes `projects/<active>/research/<filename>.md` conforming to car-template.md (DETL-02), with a Sources table covering every populated fact (DETL-05), and appends a Research Progress row to per-project state.md.
    - No tire pricing / scoring / recommendations appear (D-01/D-02 — Phase 5 scope).
  </acceptance_criteria>
  <done>The skill's second half exists: best-effort FDM + tire-size + purchase-type pricing, then a car-template-conformant file write with full sourcing and a state.md update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → skill ($ARGUMENTS car name) | Free-text car name flows into WebSearch queries and a normalized filename |
| public web → research file | Read-only fetched HTML (ev-database.org, fdm.dk, wheel-size.com, Bilbasen Blog) is summarized into the output file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-V5 | Tampering | $ARGUMENTS used in Bash(ls ...) filename | mitigate | Claude normalizes the name (lowercase, hyphenate, strip special chars) into a slug before constructing the ls path; allowed-tools restricts Bash to `ls *`; no other shell execution from raw user input (02-RESEARCH.md § Security Domain, risk LOW) |
| T-02-I | Information Disclosure | fetched public web content | accept | All sources are public, read-only pages; no auth, no API keys, no PII stored |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure Claude Code skill markdown, no external dependencies (02-RESEARCH.md: "No external packages") |
</threat_model>

<verification>
- SKILL.md exists with correct frontmatter and backtick injection.
- All 13 steps present and ordered (resolve → BRIEF → re-run guard → variant discovery → selection → ev-db fetch → FDM → tire → purchase branch → greengarage skip → write → state → confirm).
- Sourcing discipline instructions present (no training data; rated vs measured DC kW; WLTP vs real-world never averaged; every fact sourced).
- Purchase-type branch covers new / used / leasing.
- Functional behavior is validated by Plan 03 golden runs against the test projects.
</verification>

<success_criteria>
`.claude/skills/ev-detail/SKILL.md` is a complete, self-contained step-numbered skill that fetches live specs from ev-database.org (mandatory), best-effort FDM/tire/pricing, branches on purchase_type, and writes a fully-sourced per-car file conforming to car-template.md — implementing DETL-01..10, TIRE-01, OWNR-01, and SRCH-07.
</success_criteria>

<output>
Create `.planning/phases/02-detail-skill/02-02-SUMMARY.md` when done.
</output>
