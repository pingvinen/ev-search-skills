---
phase: 02-detail-skill
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - projects/ev-detail-test-new/research/volvo-ex30.md
  - projects/ev-detail-test-new/state.md
  - projects/ev-detail-test-used/research/volvo-ex30.md
  - projects/ev-detail-test-used/state.md
  - projects/ev-detail-test-leasing/research/volvo-ex30.md
  - projects/ev-detail-test-leasing/state.md
  - .planning/phases/02-detail-skill/02-VALIDATION.md
  - .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md
autonomous: false
requirements:
  - DETL-01
  - DETL-02
  - DETL-03
  - DETL-04
  - DETL-05
  - DETL-06
  - DETL-07
  - DETL-08
  - DETL-09
  - DETL-10
  - TIRE-01
  - OWNR-01
  - SRCH-07

must_haves:
  truths:
    - "A golden run of /ev-detail against the new test project produces a complete, sourced research/volvo-ex30.md"
    - "The gap-handling scenario produces a file with an explicit 'No FDM test found' note"
    - "The multi-variant scenario shows variant-selection rationale in the output"
    - "The used and leasing scenarios produce their respective price-range sections"
    - "A second run on the same car pauses and asks before overwriting (D-09)"
    - "All 5 golden-run scenarios are signed off and 02-VALIDATION.md is marked nyquist_compliant"
  artifacts:
    - path: "projects/ev-detail-test-new/research/volvo-ex30.md"
      provides: "golden-run happy-path output proving DETL-01..10, TIRE-01, OWNR-01 against a real car"
      contains: "ev-database.org"
    - path: ".planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md"
      provides: "completed sign-off checklist with each scenario's pass/fail recorded"
      contains: "Golden-Run"
  key_links:
    - from: ".claude/skills/ev-detail/SKILL.md"
      to: "projects/ev-detail-test-new/research/volvo-ex30.md"
      via: "the skill, run against the active test project, writes the per-car output"
      pattern: "research/volvo-ex30.md"
---

<objective>
Validate the `/ev-detail` skill against the 5 golden-run scenarios defined in 02-VALIDATION.md, using the test-project fixtures from Plan 01 and the skill from Plan 02. Record each scenario's result in VALIDATION-CHECKLIST.md and flip `nyquist_compliant: true` in 02-VALIDATION.md once all pass.

Purpose: This skill produces markdown (not unit-testable code), so the phase is verified observationally — by running the skill on known cars and reading the output against car-template.md. This plan is the Nyquist phase gate.
Output: golden-run research files under the test projects, a completed VALIDATION-CHECKLIST.md, and `nyquist_compliant: true` in 02-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@car-template.md
@.planning/phases/02-detail-skill/02-VALIDATION.md
@.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md
@.claude/skills/ev-detail/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 5 golden-run scenarios and record field-presence results</name>
  <files>projects/ev-detail-test-new/research/volvo-ex30.md, projects/ev-detail-test-used/research/volvo-ex30.md, projects/ev-detail-test-leasing/research/volvo-ex30.md, state.md</files>
  <read_first>
    - .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md (the scenario list and per-scenario pass conditions written by Plan 01)
    - .planning/phases/02-detail-skill/02-VALIDATION.md (the Req-ID → behavior map and golden-run scenarios)
    - .claude/skills/ev-detail/SKILL.md (the skill under test)
    - car-template.md (the output contract each produced file is checked against)
    - .planning/phases/02-detail-skill/02-RESEARCH.md (§ Validation Architecture — known EX30 facts: FDM test 2026-03-17, wheel-size.com 2024, active used market; use these as expected anchors)
  </read_first>
  <action>
    Execute the 5 golden-run scenarios from VALIDATION-CHECKLIST.md. For each, switch the active project (update the global `state.md` active_project to the relevant fixture) and run `/ev-detail` with the scenario's car, then read the produced file and check the pass conditions.

    Scenario 1 (happy path, project `ev-detail-test-new`): run `/ev-detail "Volvo EX30"`. Verify `projects/ev-detail-test-new/research/volvo-ex30.md` exists, every Specs Value cell is non-empty or explicitly marked unconfirmed, the Sources table cites an ev-database.org URL + fetch date (DETL-01/05), the Real-world range row sources to fdm.dk (DETL-06), FDM Test Notes has Styrker + Svagheder + verdict (DETL-07, OWNR-01 confidence-labeled), EV platform reads Dedicated/Adapted ICE (DETL-10), Tire size (front) is populated and sourced to wheel-size.com (TIRE-01), and Price DK tier1 + tier2 rows are populated (DETL-09). Confirm no spec value is attributable to training data (every populated cell has a Sources row — DETL-04/05).

    Scenario 2 (gap-handling): pick a car with no FDM test (confirm by running `site:fdm.dk/tests <make> <model>` first; choose one with no result). Run `/ev-detail`. Verify the FDM Test Notes section contains "No FDM test found as of <date>" and the file is still written (DETL-08, D-04).

    Scenario 3 (multi-variant): run `/ev-detail "Renault 5"` against `ev-detail-test-new`. Verify the output states the selected variant + ID + reason and lists other variants considered, and that the choice respects the BRIEF budget with the middle-tier tie-breaker (D-05/D-06).

    Scenario 4 (purchase-type branches): switch to `ev-detail-test-used`, run `/ev-detail "Volvo EX30"`, verify a low/typical/high market range sourced to a Bilbasen Blog article (SRCH-07 used, D-08). Then switch to `ev-detail-test-leasing`, run `/ev-detail "Volvo EX30"`, verify a monthly DKK range + no-residual note (SRCH-07 leasing).

    Scenario 5 (re-run guard): with `ev-detail-test-new` active and `research/volvo-ex30.md` already present from scenario 1, run `/ev-detail "Volvo EX30"` again. Verify the skill pauses and asks overwrite/skip and does not clobber silently (D-09).

    After running, restore the global `state.md` active_project to `none` (leave the repo in a clean baseline state). Note: these are live web fetches — if a transient fetch failure occurs, retry once; if a best-effort source (FDM/tire/pricing) is genuinely unavailable for the chosen car, that is a PASS for graceful-degradation behavior (D-04), not a failure — record it as observed.
  </action>
  <verify>
    <automated>test -f projects/ev-detail-test-new/research/volvo-ex30.md && grep -qi 'ev-database.org' projects/ev-detail-test-new/research/volvo-ex30.md && grep -qi 'wheel-size.com' projects/ev-detail-test-new/research/volvo-ex30.md && grep -qiE 'dedicated|adapted' projects/ev-detail-test-new/research/volvo-ex30.md && grep -qi 'DKK' projects/ev-detail-test-new/research/volvo-ex30.md</automated>
  </verify>
  <acceptance_criteria>
    - Scenario 1: `projects/ev-detail-test-new/research/volvo-ex30.md` exists; Specs rows populated or explicitly unconfirmed; Sources table cites ev-database.org + fetch date; Real-world range sources to fdm.dk; FDM Test Notes has Styrker/Svagheder/verdict; EV platform set; Tire size (front) sourced to wheel-size.com; tier1+tier2 DKK populated.
    - Scenario 2: output file for a no-FDM car contains "No FDM test found as of <date>" and is still written.
    - Scenario 3: Renault 5 output states the selected variant + reason + other variants considered.
    - Scenario 4: used run shows a Bilbasen market range (low/typical/high); leasing run shows a monthly range + no-residual note.
    - Scenario 5: a second run on the same car pauses and asks before overwriting.
    - Global state.md restored to `active_project: none` after the runs.
  </acceptance_criteria>
  <done>All 5 golden-run scenarios executed; outputs produced and observed against car-template.md.</done>
</task>

<task type="auto">
  <name>Task 2: Record sign-off and flip the nyquist flag</name>
  <files>.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md, .planning/phases/02-detail-skill/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md (the form to fill in)
    - .planning/phases/02-detail-skill/02-VALIDATION.md (the frontmatter nyquist_compliant flag and Validation Sign-Off block)
  </read_first>
  <action>
    Fill in VALIDATION-CHECKLIST.md with the observed result for each scenario's checkboxes from Task 1, noting any best-effort gaps that were graceful-degradation passes (not failures). In 02-VALIDATION.md, tick the Validation Sign-Off checkboxes and — only if all 5 scenarios passed — set `nyquist_compliant: true` and `wave_0_complete: true` in its frontmatter and change `**Approval:** pending` to approved with today's date. If any scenario genuinely failed (a mandatory behavior, not a best-effort gap), leave nyquist_compliant false and record the failure clearly for gap-closure planning.
  </action>
  <verify>
    <automated>grep -qiE 'nyquist_compliant: (true|false)' .planning/phases/02-detail-skill/02-VALIDATION.md && grep -qi 'Golden-Run' .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION-CHECKLIST.md has each scenario's checkboxes marked with observed outcomes.
    - 02-VALIDATION.md Validation Sign-Off reflects the run; `nyquist_compliant` is `true` iff all 5 scenarios passed (best-effort gaps count as passes).
    - Any genuine failure is recorded with enough detail for `/gsd-plan-phase --gaps`.
  </acceptance_criteria>
  <done>Validation is signed off and the nyquist flag reflects the true state of the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web → research file | Same read-only fetch boundary as Plan 02; this plan only exercises it via golden runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | golden-run output files + state.md | accept | Files are local test artifacts under projects/ test fixtures; no secrets, no PII; global state.md restored to baseline after runs |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — validation runs the existing skill only |
</threat_model>

<verification>
- All 5 golden-run scenarios executed and recorded.
- VALIDATION-CHECKLIST.md completed with per-scenario outcomes.
- 02-VALIDATION.md nyquist_compliant flag reflects the result.
- Global state.md restored to active_project: none.
</verification>

<success_criteria>
The `/ev-detail` skill is observationally verified against all 5 golden-run scenarios (happy path, gap-handling, multi-variant, purchase-type branches, re-run guard), proving every Phase 2 requirement (DETL-01..10, TIRE-01, OWNR-01, SRCH-07) and the key decisions (D-05/D-06 variant selection, D-09 re-run guard) on real cars; sign-off is recorded and the nyquist flag is set.
</success_criteria>

<output>
Create `.planning/phases/02-detail-skill/02-03-SUMMARY.md` when done.
</output>
