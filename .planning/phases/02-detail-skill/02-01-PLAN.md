---
phase: 02-detail-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/ev-detail-test-new/BRIEF.md
  - projects/ev-detail-test-new/state.md
  - projects/ev-detail-test-new/comparison.md
  - projects/ev-detail-test-used/BRIEF.md
  - projects/ev-detail-test-used/state.md
  - projects/ev-detail-test-used/comparison.md
  - projects/ev-detail-test-leasing/BRIEF.md
  - projects/ev-detail-test-leasing/state.md
  - projects/ev-detail-test-leasing/comparison.md
  - .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md
autonomous: true
requirements:
  - DETL-02
  - SRCH-07
user_setup: []

must_haves:
  truths:
    - "A test project with purchase_type=new exists so the EX30 happy-path golden run can target it"
    - "A test project with purchase_type=used exists so the used pricing branch can be validated"
    - "A test project with purchase_type=leasing exists so the leasing pricing branch can be validated"
    - "A repeatable validation checklist captures the 5 golden-run scenarios for sign-off"
  artifacts:
    - path: "projects/ev-detail-test-new/BRIEF.md"
      provides: "new-purchase BRIEF fixture (budget + must-haves) for variant selection and tier1/tier2 pricing golden run"
      contains: "Purchase type"
    - path: "projects/ev-detail-test-used/BRIEF.md"
      provides: "used-purchase BRIEF fixture for the Bilbasen market-range branch"
      contains: "used"
    - path: "projects/ev-detail-test-leasing/BRIEF.md"
      provides: "leasing-purchase BRIEF fixture for the monthly-range branch"
      contains: "leasing"
    - path: "projects/ev-detail-test-new/state.md"
      provides: "per-project state with empty Research Progress table the skill appends to"
      contains: "Research Progress"
    - path: ".planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md"
      provides: "repeatable sign-off checklist for the 5 golden-run scenarios"
      contains: "Golden-Run"
  key_links:
    - from: "projects/ev-detail-test-new/BRIEF.md"
      to: ".claude/skills/ev-detail/SKILL.md"
      via: "skill reads purchase_type + budget from the active project's BRIEF.md (D-05, D-07)"
      pattern: "Purchase type"
---

<objective>
Create the Wave 0 validation fixtures that the `/ev-detail` golden runs depend on: three test projects (one per `purchase_type`: new / used / leasing) and a repeatable validation checklist enumerating the 5 golden-run scenarios from 02-VALIDATION.md.

Purpose: The skill cannot be validated without an active project carrying a BRIEF.md (variant selection per D-05 and purchase-type branching per D-07 both read BRIEF.md). 02-VALIDATION.md Wave 0 requires a test project + optional checklist before any golden run. This plan front-loads them so Plan 03 can run the scenarios deterministically.
Output: `projects/ev-detail-test-{new,used,leasing}/` scaffolds (BRIEF.md, state.md, comparison.md each) and `.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detail-skill/02-VALIDATION.md
@.claude/skills/ev-new-project/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three test-project scaffolds (new / used / leasing)</name>
  <files>projects/ev-detail-test-new/BRIEF.md, projects/ev-detail-test-new/state.md, projects/ev-detail-test-new/comparison.md, projects/ev-detail-test-used/BRIEF.md, projects/ev-detail-test-used/state.md, projects/ev-detail-test-used/comparison.md, projects/ev-detail-test-leasing/BRIEF.md, projects/ev-detail-test-leasing/state.md, projects/ev-detail-test-leasing/comparison.md</files>
  <read_first>
    - .claude/skills/ev-new-project/SKILL.md (the BRIEF.md template and per-project state.md template are defined at the bottom of this file — copy their exact structure; the budget subsection differs per purchase type)
    - state.md (repo root — shows the global state shape; do NOT change active_project here, Plan 03 does that at golden-run time)
    - .planning/phases/02-detail-skill/02-CONTEXT.md (D-05 variant selection reads budget/range/must-haves; D-07 branches on purchase_type)
  </read_first>
  <action>
    Create three project directories under `projects/`, each a self-contained fixture mirroring exactly what `/ev-new-project` would produce (PROJ-01 schema). Use the BRIEF.md template and per-project state.md template found at the bottom of `.claude/skills/ev-new-project/SKILL.md` verbatim, filling the slots described below. For each project write three files: `BRIEF.md`, `state.md`, `comparison.md`.

    Project `ev-detail-test-new` (Purchase type: new): set Budget "Preferred range" 300000 DKK and "Maximum" 350000 DKK; Must-Have Features list "Electric only (BEV)" plus "wireless Android Auto"; Body Type "SUV / crossover"; Seats "4-5 seats". This fixture drives the EX30 happy-path golden run (variant selection per D-05/D-06 and tier1/tier2 new pricing per DETL-09).

    Project `ev-detail-test-used` (Purchase type: used): include the used budget subsection only — "Preferred range" 220000 DKK, "Maximum" 260000 DKK, "Max age" 3 years, "Max km" 60000. Same must-haves/body/seats as above. This fixture drives the used market-range branch (D-07 used → Bilbasen low/typical/high).

    Project `ev-detail-test-leasing` (Purchase type: leasing): include the leasing budget subsection only — "Monthly budget" 4500 DKK/mo, "Max upfront" 25000 DKK, "Lease term" 36 months. Same must-haves/body/seats. This fixture drives the leasing branch (D-07 leasing → monthly payment range).

    For each project's `state.md`: use the per-project state template (Created = today's date 2026-06-22, Status active, correct Purchase type) and leave the Research Progress table empty with only its header row — the skill appends to it at golden-run time. For each `comparison.md`: the same placeholder content `/ev-new-project` writes ("No cars researched yet. Run /ev-detail to add cars.").

    Do NOT modify the global repo-root `state.md` (active_project stays `none`); the golden-run plan switches active project per scenario. Do NOT invent fields not present in the ev-new-project templates.
  </action>
  <verify>
    <automated>test -f projects/ev-detail-test-new/BRIEF.md && test -f projects/ev-detail-test-used/BRIEF.md && test -f projects/ev-detail-test-leasing/BRIEF.md && grep -qi 'new' projects/ev-detail-test-new/BRIEF.md && grep -qi 'used' projects/ev-detail-test-used/BRIEF.md && grep -qi 'leasing' projects/ev-detail-test-leasing/BRIEF.md && grep -q 'Research Progress' projects/ev-detail-test-new/state.md</automated>
  </verify>
  <acceptance_criteria>
    - Three directories exist: `projects/ev-detail-test-new/`, `.../-used/`, `.../-leasing/`, each containing BRIEF.md, state.md, comparison.md.
    - Each BRIEF.md declares the matching `**Purchase type:**` and contains ONLY the budget subsection for that purchase type (new = preferred/max; used = preferred/max/age/km; leasing = monthly/upfront/term).
    - Each per-project state.md has a `## Research Progress` table header with no data rows yet, plus the Source Reliability Notes and Discovered Sources sections.
    - Global repo-root `state.md` is unchanged (`active_project: none`).
  </acceptance_criteria>
  <done>Three purchase-type test-project fixtures exist matching the ev-new-project schema, ready for golden runs.</done>
</task>

<task type="auto">
  <name>Task 2: Write the golden-run validation checklist</name>
  <files>.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/02-detail-skill/02-VALIDATION.md (the 5 golden-run scenarios and the Req-ID → behavior map are defined here — the checklist restates them as a sign-off form)
    - car-template.md (the output contract whose rows the checklist asserts are populated)
  </read_first>
  <action>
    Write `.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md`: a repeatable, checkbox-driven sign-off form covering the 5 golden-run scenarios from 02-VALIDATION.md (1: Volvo EX30 happy path against `ev-detail-test-new`; 2: gap-handling with a no-FDM car; 3: multi-variant Renault 5 selection reasoning; 4: purchase-type branches using the `-used` and `-leasing` fixtures; 5: re-run overwrite prompt per D-09).

    For each scenario list: which test project to switch to, the exact `/ev-detail "<car>"` invocation, and the observable pass conditions phrased as checkboxes (e.g., for scenario 1 — `research/volvo-ex30.md` exists; every Specs row has a non-empty Value; Sources table cites an ev-database.org URL with fetch date; Real-world range row sources to fdm.dk; FDM Test Notes has Styrker and Svagheder; EV platform field reads Dedicated or Adapted ICE; Tire size (front) populated and sourced to wheel-size.com; tier1 and tier2 DKK rows populated). Map each checkbox to the requirement ID it verifies (DETL-01..10, TIRE-01, OWNR-01, SRCH-07, D-05/D-06, D-09) so sign-off is traceable.

    Include a short "How to run" preamble (switch active project, run the skill, read the produced file against car-template.md) and a final sign-off block (all 5 scenarios pass → set `nyquist_compliant: true` in 02-VALIDATION.md). Do not duplicate the full research; reference 02-VALIDATION.md and 02-RESEARCH.md § Validation Architecture for detail.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md && grep -qi 'Golden-Run' .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md && grep -q 'DETL-0' .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md` exists with a "Golden-Run" section enumerating all 5 scenarios.
    - Each scenario names the test project to switch to, the exact `/ev-detail` invocation, and checkbox pass conditions.
    - Each checkbox is traceable to at least one requirement/decision ID (DETL-01..10, TIRE-01, OWNR-01, SRCH-07, D-05/D-06, D-09).
    - A final sign-off block exists referencing the `nyquist_compliant: true` flip in 02-VALIDATION.md.
  </acceptance_criteria>
  <done>A repeatable validation checklist exists that Plan 03 follows to sign off all 5 golden-run scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | This plan only writes local markdown fixtures and a checklist; no untrusted input crosses any boundary, no network, no package installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | local fixture files under projects/ | accept | Files are static test fixtures with no secrets and no executable content; low-value, no PII |
</threat_model>

<verification>
- All three test-project directories exist with BRIEF.md, state.md, comparison.md.
- BRIEF.md budget subsections match their declared purchase type.
- VALIDATION-CHECKLIST.md enumerates all 5 golden-run scenarios with requirement traceability.
- Global repo-root state.md unchanged.
</verification>

<success_criteria>
Three purchase-type test-project fixtures and a traceable golden-run checklist exist, so Plan 03 can validate `/ev-detail` deterministically across new / used / leasing branches.
</success_criteria>

<output>
Create `.planning/phases/02-detail-skill/02-01-SUMMARY.md` when done.
</output>