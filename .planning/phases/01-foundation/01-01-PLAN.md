---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - car-template.md
  - state.md
autonomous: true
requirements:
  - SRCH-04
  - SRCH-05

must_haves:
  truths:
    - "car-template.md exists at repo root with separate WLTP and real-world range rows"
    - "car-template.md has a Sources table with URL and Fetch date columns"
    - "car-template.md has HTML comment guidance for EV platform origin and range distinction"
    - "car-template.md has fields for tier 1 and tier 2 DKK pricing"
    - "global state.md exists at repo root with active_project field"
  artifacts:
    - path: "car-template.md"
      provides: "Per-car output template for ev-detail skill (Phase 2)"
      contains: "WLTP range"
    - path: "state.md"
      provides: "Global state tracking active project"
      contains: "active_project"
  key_links:
    - from: "car-template.md"
      to: "Phase 2 ev-detail skill"
      via: "Template reference for output format"
      pattern: "WLTP range.*Real-world range"
    - from: "state.md"
      to: ".claude/skills/ev-switch-project/SKILL.md"
      via: "Backtick injection reads active_project"
      pattern: "active_project:"
---

<objective>
Create the data contract files that all downstream skills depend on: the per-car output template (`car-template.md`) and the global state file (`state.md`).

Purpose: These files define the schema that Phase 2's ev-detail skill writes to and all skills read from. Getting them right now prevents format drift later.
Output: `car-template.md` at repo root, `state.md` at repo root
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@search_criteria.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create car-template.md per-car output template</name>
  <files>car-template.md</files>
  <read_first>
    - search_criteria.md (content reference for what fields matter to this user)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: car-template.md with HTML Guidance Comments — lines 236-305)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-07, D-08)
  </read_first>
  <action>
Create `car-template.md` at repository root. This is the reference template that Phase 2's `/ev-detail` skill will use when writing per-car research files to `projects/<name>/research/<make-model>.md`.

The file must contain exactly these elements (per D-07: section headings with inline HTML comment guidance, not rigid field tables; per D-08: EV platform origin and WLTP/real-world range distinction are guidance notes within Specs, not separate sections):

**Header:**
```
# [Make Model Variant]

**Researched:** [date]
**Project:** [project name]
```

**## Quick Verdict** section with HTML comment: `<!-- 2-3 sentences: does it meet the BRIEF criteria, what stands out -->`

**## Specs** section containing:
- An HTML comment block that explicitly states: "RANGE: Always report WLTP range AND real-world range separately. Never mix or average them. WLTP = manufacturer-claimed standardized test figure. Real-world = FDM measured at 110 km/h, or ev-database.org 'mild weather' figure. If only one is available, state which and note the other is missing."
- A markdown table with these exact rows (each with Value and Source columns):
  - WLTP range (km)
  - Real-world range (mild) (km)
  - Real-world range (cold) (km)
  - Battery (usable) (kWh)
  - DC charge peak (kW)
  - AC charge rate (kW)
  - 10-80% DC charge time (min)
  - 0-100 km/h (s)
  - Cargo (L)
  - Tow capacity (kg)
  - Tire size (front)
  - Tire size (rear)
  - Price DK tier 1 (from) (DKK)
  - Price DK tier 2 (best value) (DKK)
  - Power output (kW)
- An HTML comment for EV platform: `<!-- EV PLATFORM: Note whether this car is built on a dedicated EV platform or an adapted ICE platform. Example: "Dedicated EV platform (MEB)" or "Adapted ICE platform (PF1)" -->`
- A bold line: `**EV platform:** [dedicated / adapted ICE + platform name]`

**## FDM Test Notes** section with HTML comment about handling present vs missing FDM tests.

**## Tire Research** section with HTML comment about capturing tire size and price for quality all-season set.

**## Ownership Signals** section with HTML comment about reliability, known issues, confidence labels.

**## Danish Market Context** section with HTML comment about registration tax, insurance tier.

**## Pros** section with a single `-` bullet placeholder.

**## Cons** section with a single `-` bullet placeholder.

**## Sources** section with HTML comment requiring every fact to trace to URL and fetch date, plus a markdown table:
```
| Claim | Source URL | Fetch date |
|-------|-----------|------------|
| | | |
```
  </action>
  <verify>
    <automated>grep -c "WLTP range" /Users/user/workspace/github/car-research/car-template.md && grep -c "Real-world range" /Users/user/workspace/github/car-research/car-template.md && grep -c "Source URL" /Users/user/workspace/github/car-research/car-template.md && grep -c "EV PLATFORM" /Users/user/workspace/github/car-research/car-template.md && grep -c "tier 1" /Users/user/workspace/github/car-research/car-template.md && grep -c "tier 2" /Users/user/workspace/github/car-research/car-template.md</automated>
  </verify>
  <acceptance_criteria>
    - car-template.md exists at repo root
    - File contains `| WLTP range |` as a table row
    - File contains `| Real-world range (mild) |` as a separate table row (not combined with WLTP)
    - File contains `| Real-world range (cold) |` as a separate table row
    - File contains `| Price DK tier 1 (from) |` table row
    - File contains `| Price DK tier 2 (best value) |` table row
    - File contains `| Power output |` table row
    - File contains `<!-- EV PLATFORM:` HTML comment
    - File contains `**EV platform:**` bold line
    - File contains `<!-- RANGE: Always report WLTP range AND real-world range separately` HTML comment
    - File contains `## Sources` section with `| Source URL | Fetch date |` table headers
    - File contains `## FDM Test Notes` section
    - File contains `## Tire Research` section
    - File contains `## Ownership Signals` section
    - File contains `## Danish Market Context` section
    - File contains `| Field | Value | Source |` as the Specs table header
  </acceptance_criteria>
  <done>car-template.md exists at repo root with all required sections, separate WLTP/real-world range rows, tier 1/tier 2 pricing rows, EV platform guidance, and a Sources table with URL + fetch date columns</done>
</task>

<task type="auto">
  <name>Task 2: Create global state.md</name>
  <files>state.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 6: Global state.md Format — lines 307-329)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-05, D-06)
  </read_first>
  <action>
Create `state.md` at repository root. This is the global state file that all skills read via backtick injection (`!`cat state.md 2>/dev/null || echo "No active project set"``).

Per D-05: two-level state model — this file is the global level (per-project state.md is created by ev-new-project in Plan 02).

The file must contain:

1. YAML frontmatter with:
   - `active_project: none` (no project active initially)
   - `last_updated: 2026-03-25`

2. Markdown body with:
   - `# Global State` heading
   - `## Active Project` section with:
     - `**Project:** none`
     - `**Switched:** 2026-03-25`
   - `## Tool Notes` section with HTML comment: `<!-- Any cross-project observations, source reliability notes -->`

This exact format — YAML frontmatter `active_project:` field is what skills will parse via backtick injection. The markdown body is human-readable context.
  </action>
  <verify>
    <automated>grep "active_project:" /Users/user/workspace/github/car-research/state.md && grep "last_updated:" /Users/user/workspace/github/car-research/state.md && grep "## Active Project" /Users/user/workspace/github/car-research/state.md && grep "## Tool Notes" /Users/user/workspace/github/car-research/state.md</automated>
  </verify>
  <acceptance_criteria>
    - state.md exists at repo root
    - File contains YAML frontmatter delimiters (`---` on lines 1 and 4)
    - File contains `active_project: none` in frontmatter
    - File contains `last_updated:` in frontmatter
    - File contains `# Global State` heading
    - File contains `## Active Project` section
    - File contains `**Project:** none`
    - File contains `## Tool Notes` section
  </acceptance_criteria>
  <done>state.md exists at repo root with YAML frontmatter containing active_project field, human-readable sections for Active Project and Tool Notes</done>
</task>

</tasks>

<verification>
- `car-template.md` has separate rows for WLTP range and Real-world range (mild/cold) — grep confirms both present
- `car-template.md` has Sources section with URL and fetch date columns
- `car-template.md` has HTML comment guidance for range distinction and EV platform
- `state.md` has `active_project:` in YAML frontmatter
- Both files exist at repo root
</verification>

<success_criteria>
- car-template.md exists with all 15 spec table rows, HTML guidance comments, and Sources table
- state.md exists with active_project field readable by backtick injection
- No hardcoded project names in either file
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
