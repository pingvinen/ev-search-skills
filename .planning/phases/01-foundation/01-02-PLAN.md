---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .claude/skills/ev-new-project/SKILL.md
  - .claude/skills/ev-switch-project/SKILL.md
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - SRCH-01
  - SRCH-06

must_haves:
  truths:
    - "Running /ev-new-project creates projects/<name>/ with BRIEF.md, research/ folder, comparison.md, and state.md"
    - "Running /ev-new-project with an existing name errors and suggests /ev-switch-project"
    - "Running /ev-switch-project updates state.md active_project field"
    - "When no active project is set, skills list existing projects and prompt the user"
    - "Active project context persists across invocations via state.md on disk"
  artifacts:
    - path: ".claude/skills/ev-new-project/SKILL.md"
      provides: "Project creation skill with scaffolding and guardrails"
      contains: "ev-new-project"
    - path: ".claude/skills/ev-switch-project/SKILL.md"
      provides: "Project switching skill that updates global state"
      contains: "ev-switch-project"
  key_links:
    - from: ".claude/skills/ev-new-project/SKILL.md"
      to: "state.md"
      via: "Writes active_project after creating project"
      pattern: "active_project"
    - from: ".claude/skills/ev-switch-project/SKILL.md"
      to: "state.md"
      via: "Backtick injection reads then skill updates active_project"
      pattern: "cat state.md"
    - from: ".claude/skills/ev-new-project/SKILL.md"
      to: "projects/$ARGUMENTS/BRIEF.md"
      via: "Creates BRIEF.md from embedded template"
      pattern: "BRIEF.md"
---

<objective>
Create the two project management skills (`/ev-new-project` and `/ev-switch-project`) that manage project lifecycle and active project context.

Purpose: These skills enable multi-project research -- users can create isolated research contexts and switch between them. All downstream skills (ev-detail, ev-search, ev-compare) depend on having an active project.
Output: `.claude/skills/ev-new-project/SKILL.md`, `.claude/skills/ev-switch-project/SKILL.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@state.md
@car-template.md
@search_criteria.md
</context>

<interfaces>
<!-- Key contracts from Plan 01 that this plan depends on -->

From state.md (created by Plan 01):
The global state file has YAML frontmatter with `active_project: none` and `last_updated: 2026-03-25`, followed by markdown body with `# Global State`, `## Active Project` (with `**Project:** none` and `**Switched:** 2026-03-25`), and `## Tool Notes`.

From car-template.md (created by Plan 01):
The per-car output template with Specs table, Sources table, and section headings.
Not directly consumed by these skills, but ev-new-project's BRIEF.md template must align with the research output format.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create /ev-new-project skill</name>
  <files>.claude/skills/ev-new-project/SKILL.md</files>
  <read_first>
    - state.md (the global state file format this skill must update)
    - search_criteria.md (content reference for BRIEF.md template -- shows what criteria the user actually cares about)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: ev-new-project Guardrails lines 160-186; Pattern 4: BRIEF.md Schema lines 188-228; Pattern 7: Per-Project state.md lines 331-355; Pitfall 3 lines 402-408; Pitfall 4 lines 410-418)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01 through D-04, D-09, D-10)
  </read_first>
  <action>
Create directory `.claude/skills/ev-new-project/` and write `SKILL.md` inside it.

This file is a Claude Code skill. It has YAML frontmatter (delimited by triple-dashes) followed by a prompt body. The executor must write this as a single file that Claude Code will parse as a skill.

FRONTMATTER (the exact YAML fields between triple-dash delimiters):
- name: ev-new-project
- description: Create a new EV research project. Use when the user wants to start researching a new set of cars with different criteria.
- allowed-tools: Write, Read, Bash(mkdir *, ls *)
- disable-model-invocation: true
- argument-hint: [project-name]

BODY (the prompt text after the closing frontmatter delimiter):

The body must contain these elements in order:

1. BACKTICK INJECTION to read current state. The line must be:
   `!`cat state.md 2>/dev/null || echo "state.md not found"``
   This runs before Claude sees the prompt, injecting the state.md content.

2. PROJECT NAME from arguments:
   `Project name requested: $ARGUMENTS`

3. GUARDRAIL INSTRUCTIONS -- numbered steps Claude follows:

   Step 1 (per D-10): Check if `projects/$ARGUMENTS/` already exists using `Bash(ls projects/$ARGUMENTS/ 2>/dev/null)`. If it EXISTS: stop immediately, tell the user the project already exists, suggest `/ev-switch-project "$ARGUMENTS"` instead. Never overwrite.

   Step 2 (per D-02): Ask the user: "What is the purchase type for this project? Options: `new`, `used`, or `leasing`. Default is `new` if you don't specify."

   Step 3: Create the project scaffold:
   - Run `Bash(mkdir -p projects/$ARGUMENTS/research)`
   - Write `projects/$ARGUMENTS/BRIEF.md` using the BRIEF template below, with the purchase type from Step 2 filled in. IMPORTANT per D-02: only scaffold the ONE budget section matching the chosen purchase type, not all three.
   - Write `projects/$ARGUMENTS/state.md` with initial per-project state (format below)
   - Write `projects/$ARGUMENTS/comparison.md` with placeholder content: heading "# Comparison: [project name]" followed by "No cars researched yet. Run /ev-detail to add cars."

   Step 4: Update global `state.md` -- set `active_project:` to `$ARGUMENTS` in both the YAML frontmatter and the markdown body. Update `last_updated` and `Switched` date.

   Step 5: Confirm creation to the user -- list what was created and note the active project is now set.

4. BRIEF.MD TEMPLATE (embedded inline, per D-01: markdown prose format):

The template that gets written to `projects/$ARGUMENTS/BRIEF.md`:

Heading: `# Project Brief: [Project Name]`
Bold field: `**Purchase type:** [new/used/leasing]`

Section `## Context` with HTML comment guidance: "Why this search, what's being replaced, key constraints"

Section `## Budget` with ONE of these subsections based on purchase type:
- For "new": fields `**Preferred range:** [amount] DKK` and `**Maximum:** [amount] DKK`
- For "used": fields `**Preferred range:** [amount] DKK`, `**Maximum:** [amount] DKK`, `**Max age:** [years]`, `**Max km:** [km]`
- For "leasing": fields `**Monthly budget:** [amount] DKK/mo`, `**Max upfront:** [amount] DKK`, `**Lease term:** [months]`

Subsection `### Per-brand overrides` with HTML comment: "Percentage uplift from base budget, per D-03. Example: [Brand]: +50% (discount arrangement)"

Section `## Must-Have Features` with HTML comment "Hard requirements that filter results" and starter bullet `- Electric only (BEV)`

Section `## Body Type` with HTML comment guidance

Section `## Seats` with HTML comment guidance

Section `## Preferred Features` with HTML comment "Nice-to-haves that don't filter but inform comparison"

Section `## Brand Notes` with HTML comment "Per-brand research flags, exclusions, quality concerns"

5. PER-PROJECT STATE.MD TEMPLATE:

The template that gets written to `projects/$ARGUMENTS/state.md`:

Heading: `# Project State: [project name]`
Bold fields: `**Created:** [date]`, `**Purchase type:** [from Step 2]`, `**Status:** active`

Section `## Research Progress` with a markdown table: columns `Car model | File | Researched | FDM found` (empty initially)

Section `## Source Reliability Notes` with HTML comment "Fetch-time observations for this project's research session"

Section `## Discovered Sources` with HTML comment "Useful URLs found during research that weren't in the initial brief"
  </action>
  <verify>
    <automated>test -f /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && grep "disable-model-invocation: true" /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && grep "ev-new-project" /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && grep "BRIEF.md" /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && grep "already exists" /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && grep "purchase type" /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && grep "ev-switch-project" /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && grep 'cat state.md' /Users/user/workspace/github/car-research/.claude/skills/ev-new-project/SKILL.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/ev-new-project/SKILL.md exists
    - File contains frontmatter with `name: ev-new-project`
    - File contains `disable-model-invocation: true` in frontmatter
    - File contains `allowed-tools:` with Write, Read, and Bash
    - File contains `argument-hint: [project-name]`
    - File contains backtick injection: `cat state.md 2>/dev/null`
    - File contains `$ARGUMENTS` for project name substitution
    - File contains existence check instruction referencing `ls projects/$ARGUMENTS`
    - File contains "already exists" error message directing user to `/ev-switch-project`
    - File contains "purchase type" question with options `new`, `used`, `leasing`
    - File contains BRIEF.md template with `## Budget` section
    - File contains `### Per-brand overrides` subsection with percentage uplift example (per D-03)
    - File contains `## Must-Have Features` section (for SRCH-05)
    - File contains per-project state.md template with `## Research Progress` table
    - File contains instruction to update global `state.md` with `active_project:`
    - File contains instruction to create `projects/$ARGUMENTS/comparison.md`
    - File contains instruction to run `mkdir -p projects/$ARGUMENTS/research`
    - File contains instruction to scaffold only ONE budget section matching purchase type (per D-02)
  </acceptance_criteria>
  <done>/ev-new-project skill exists with guardrails (D-09, D-10), BRIEF.md template (D-01, D-02, D-03, D-04), per-project state.md template, and global state.md update logic</done>
</task>

<task type="auto">
  <name>Task 2: Create /ev-switch-project skill</name>
  <files>.claude/skills/ev-switch-project/SKILL.md</files>
  <read_first>
    - state.md (the global state file this skill reads and updates)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: Backtick Injection for Active Project lines 118-139; Pitfall 1 lines 384-390)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-05, D-06, D-09, D-11)
  </read_first>
  <action>
Create directory `.claude/skills/ev-switch-project/` and write `SKILL.md` inside it.

This file is a Claude Code skill. It has YAML frontmatter (delimited by triple-dashes) followed by a prompt body.

FRONTMATTER (the exact YAML fields between triple-dash delimiters):
- name: ev-switch-project
- description: Switch the active EV research project. Use when the user says "switch to project X" or "change project".
- allowed-tools: Write, Read, Bash(ls *)
- disable-model-invocation: true
- argument-hint: [project-name]

BODY (the prompt text after the closing frontmatter delimiter):

The body must contain these elements in order:

1. BACKTICK INJECTION to read current state. Two lines:
   `Current global state:`
   `!`cat state.md 2>/dev/null || echo "state.md not found -- no active project"``

2. TARGET PROJECT from arguments:
   `Switch to project: $ARGUMENTS`

3. INSTRUCTIONS -- numbered steps Claude follows:

   Step 1: Verify that `projects/$ARGUMENTS/` exists using `Bash(ls projects/$ARGUMENTS/ 2>/dev/null)`.
   - If it does NOT exist: list existing projects with `Bash(ls projects/ 2>/dev/null || echo "No projects found")`. Tell the user the project doesn't exist, show available projects, and suggest `/ev-new-project "$ARGUMENTS"` to create it. Stop here.

   Step 2: Update global `state.md`:
   - Read the current `state.md`
   - Change `active_project:` in YAML frontmatter to `$ARGUMENTS`
   - Change `**Project:**` in the Active Project section to `$ARGUMENTS`
   - Update `last_updated:` in frontmatter and `**Switched:**` in body to today's date

   Step 3: Confirm the switch to the user. Show:
   - Previous active project (from the state read in backtick injection)
   - New active project: $ARGUMENTS
   - Quick summary of project contents (list files in `projects/$ARGUMENTS/research/` if any exist)

4. NO-ARGUMENT HANDLING (per D-09): If `$ARGUMENTS` is empty or not provided:
   - List existing projects with `Bash(ls projects/ 2>/dev/null || echo "No projects found")`
   - Ask the user which project to switch to
   - If no projects exist, suggest `/ev-new-project` to create one
  </action>
  <verify>
    <automated>test -f /Users/user/workspace/github/car-research/.claude/skills/ev-switch-project/SKILL.md && grep "disable-model-invocation: true" /Users/user/workspace/github/car-research/.claude/skills/ev-switch-project/SKILL.md && grep "ev-switch-project" /Users/user/workspace/github/car-research/.claude/skills/ev-switch-project/SKILL.md && grep 'cat state.md' /Users/user/workspace/github/car-research/.claude/skills/ev-switch-project/SKILL.md && grep "active_project" /Users/user/workspace/github/car-research/.claude/skills/ev-switch-project/SKILL.md && grep "ev-new-project" /Users/user/workspace/github/car-research/.claude/skills/ev-switch-project/SKILL.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/ev-switch-project/SKILL.md exists
    - File contains frontmatter with `name: ev-switch-project`
    - File contains `disable-model-invocation: true` in frontmatter
    - File contains `allowed-tools:` with Write, Read, and Bash (or `Bash(ls *)`)
    - File contains `argument-hint: [project-name]`
    - File contains backtick injection: `cat state.md 2>/dev/null`
    - File contains `$ARGUMENTS` for project name substitution
    - File contains existence check for `projects/$ARGUMENTS/`
    - File contains fallback to list existing projects when target doesn't exist
    - File contains suggestion to use `/ev-new-project` when project doesn't exist
    - File contains instruction to update `active_project:` in state.md
    - File contains no-argument handling that lists projects and prompts user (per D-09)
  </acceptance_criteria>
  <done>/ev-switch-project skill exists with state.md update logic (D-06), missing-project guardrail (D-09), no-argument handling, and project existence verification</done>
</task>

</tasks>

<verification>
- Both skill files exist in `.claude/skills/` with correct directory structure
- Both have `disable-model-invocation: true` (prevents auto-triggering)
- ev-new-project has BRIEF.md template with purchase type, budget overrides, must-have features
- ev-new-project has existence check guardrail (D-10)
- ev-switch-project has state.md update logic
- Both use backtick injection to read state.md
- Both handle missing/empty state gracefully
</verification>

<success_criteria>
- `/ev-new-project` skill file exists and contains project creation logic with all guardrails
- `/ev-switch-project` skill file exists and contains state switching logic
- Both skills reference state.md via backtick injection
- BRIEF.md template in ev-new-project covers budget, per-brand overrides (SRCH-04), must-have features (SRCH-05), and purchase type
- Project isolation enforced by path construction from active_project (PROJ-03)
- State persists to disk via state.md (PROJ-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
