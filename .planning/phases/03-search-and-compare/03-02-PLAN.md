---
phase: 03-search-and-compare
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/ev-compare/SKILL.md
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
user_setup: []

must_haves:
  truths:
    - "Running /ev-compare reads all of the active project's research/*.md files (COMP-01)"
    - "It writes projects/<active>/comparison.md with one column per car (COMP-01, COMP-03)"
    - "WLTP range and FDM real-world range appear as separate, per-column-labelled rows — never merged (COMP-02, D-16)"
    - "Best-in-class is highlighted per row and a brief-aware verdict sits at the top (D-14)"
    - "Gaps render as explicit text (e.g. 'no FDM test'), never blank or guessed (D-17)"
  artifacts:
    - path: ".claude/skills/ev-compare/SKILL.md"
      provides: "/ev-compare skill: Glob research/*.md -> side-by-side comparison.md with labelled range rows"
      min_lines: 70
      contains: "name: ev-compare"
  key_links:
    - from: ".claude/skills/ev-compare/SKILL.md"
      to: "projects/<active>/research/*.md"
      via: "Glob scoped to the resolved active_project (COMP-01, Pitfall 5)"
      pattern: "research/\\*.md"
    - from: ".claude/skills/ev-compare/SKILL.md"
      to: "projects/<active>/comparison.md"
      via: "single Write of the generated comparison table (COMP-03)"
      pattern: "comparison.md"
    - from: ".claude/skills/ev-compare/SKILL.md"
      to: "range methodology labels"
      via: "separate WLTP and FDM real-world rows (D-16, COMP-02)"
      pattern: "Real-world range \\(cold\\)"
---

<objective>
Author the `/ev-compare` skill at `.claude/skills/ev-compare/SKILL.md`. It resolves the active project, Globs `projects/<active>/research/*.md`, reads each per-car file, and writes `projects/<active>/comparison.md` as decision support: the ~15 spec rows from `car-template.md` plus condensed qualitative rows, one column per car, best-in-class highlighted per row, a brief-aware verdict at the top (D-14, D-15). WLTP range and FDM real-world range stay as separate, per-column-labelled rows (D-16, COMP-02). Gaps render explicitly, never blank (D-17). It is a write-side skill: `disable-model-invocation: true`, tools `Read, Write, Glob`, active-project-scoped reads only (D-18, PROJ-03).

Purpose: deliver COMP-01 (read all research/*.md, generate side-by-side table), COMP-02 (label range methodology per column), COMP-03 (write to comparison.md).
Output: `.claude/skills/ev-compare/SKILL.md`, and at runtime `projects/<active>/comparison.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-and-compare/03-CONTEXT.md
@.planning/phases/03-search-and-compare/03-RESEARCH.md
@.planning/phases/03-search-and-compare/03-PATTERNS.md
</context>

<artifacts>
## Artifacts this phase produces (this plan)

- New file: `.claude/skills/ev-compare/SKILL.md`
- New command: `/ev-compare` (no arguments — reads the active project's research/*.md)
- Runtime output: writes/overwrites `projects/<active>/comparison.md`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Author the ev-compare SKILL.md</name>
  <files>.claude/skills/ev-compare/SKILL.md</files>
  <read_first>
    - .claude/skills/ev-research/SKILL.md (PRIMARY FRONTMATTER ANALOG — lines 1-6: write-side frontmatter with `disable-model-invocation: true`)
    - .claude/skills/ev-new-project/SKILL.md (Write-access skill, single-Write output pattern)
    - .claude/skills/ev-switch-project/SKILL.md (line 10 backtick injection without `$ARGUMENTS`)
    - .claude/skills/ev-detail/SKILL.md (Step 1 active-project guard at lines 26-33 — reuse verbatim)
    - car-template.md (the per-car file contract /ev-compare reads — the 15 Specs rows at lines 17-33, the `**EV platform:**` line, FDM Test Notes, Ownership Signals, Pros, Cons; defines the separate WLTP vs real-world range rows = the D-16 contract)
    - projects/test-ev-detail-new/research/volvo-ex30.md (a live per-car file showing exactly how rows/sections are populated, including gap notes like the Renault tire 'unconfirmed' case)
    - .planning/phases/03-search-and-compare/03-RESEARCH.md (Pattern 5 lines 363-397 = comparison table layout + best-in-class marking + gap rendering; Code Examples lines 514-524 = the labelled range-row header format; Pitfalls 4-6)
    - .planning/phases/03-search-and-compare/03-PATTERNS.md (ev-compare Pattern Assignments: frontmatter, Glob->Read loop, table output, Write output)
    - .planning/phases/03-search-and-compare/03-CONTEXT.md (locked decisions D-14..D-18)
  </read_first>
  <action>
    Create `.claude/skills/ev-compare/SKILL.md` mirroring the existing write-side skills. Structure it as ordered, numbered, titled steps with the standard "Follow these steps in order." directive.

    Frontmatter: `name: ev-compare`; a `description:` covering "generate a comparison table from researched cars; reads research/*.md and writes comparison.md; must be invoked explicitly"; `allowed-tools: Read, Write, Glob`; `disable-model-invocation: true` (D-18, Pitfall 4 — mandatory so Claude never auto-overwrites comparison.md). No `argument-hint`, no `$ARGUMENTS` line.

    Backtick injection: copy ev-switch-project line 10 verbatim (`!`cat state.md ...``).

    Step 1 — Resolve active project: copy ev-detail Step 1 guard (lines 26-33). Stop with the standard "no active project" message if active_project is `none` or missing.

    Step 2 — Discover research files (COMP-01): Glob `projects/<active_project>/research/*.md`. The Glob path MUST be constructed from the resolved active_project — never hardcoded and never cross-project (D-18, PROJ-03, Pitfall 5). If Glob returns zero files, stop and tell the user to research cars first (suggest /ev-search then /ev-research). If only one file, note that a comparison of one car is trivial but still produce the single-column table.

    Step 3 — Read each file and extract fields: Read every path from Step 2. From each, extract all 15 Specs rows (per car-template.md lines 17-33), the `**EV platform:**` line, the FDM Test Notes (verdict one-liner, top Styrker, top Svagheder), Ownership Signals confidence, and Pros/Cons — the condensed qualitative signals (D-15).

    Step 4 — Build the comparison table (COMP-01, COMP-02, D-14, D-16, D-17): one column per car. Use the row set from RESEARCH Pattern 5 (lines 370-393) and PATTERNS lines 205-228. CRITICAL (D-16, COMP-02): keep "WLTP range (km)", "Real-world range (mild) (km)", and "Real-world range (cold) (km)" as THREE SEPARATE rows, each with its methodology labelled in the row name in parentheses — WLTP labelled "(manufacturer rated)", the real-world rows labelled with the FDM measurement conditions (e.g. "(FDM 110km/h, 20°C)") or "(EVDB estimate)" when the figure came from ev-database rather than an FDM test. NEVER merge or average WLTP and real-world (Anti-Pattern). Mark best-in-class per row (bold the best value — highest for range/battery/cargo/tow, lowest for charge time/0-100/price). Render gaps as explicit text — `no FDM test`, `unconfirmed`, `not available (<purchase_type>)` — never blank or guessed (D-17).

    Step 5 — Add the brief-aware verdict + qualitative rows (D-14, D-15): at the top of the file, a `## Brief-Aware Verdict` with 2-3 sentences naming the best fit for this brief and why, reasoned from the data (read `projects/<active_project>/brief.md` for the budget/body/range criteria to reason against). Below the spec table, include condensed qualitative rows: FDM verdict one-liner, top 2 Styrker, top 2 Svagheder, ownership confidence per car.

    Step 6 — Write comparison.md (COMP-03): single Write to `projects/<active_project>/comparison.md`, overwriting unconditionally (the skill is explicit-invoke only, so no overwrite guard is needed — D-18). Header block: `# Comparison: <active_project>`, generated date, cars compared (count + names), and the sort key used (Claude's discretion — e.g. WLTP range descending or tier-1 price ascending). Then the verdict, then the spec table.

    Step 7 — Confirm to the user: report the file written, the number of cars compared, the sort key, and any cars with notable gaps.

    Do NOT inline a full example comparison file body in the skill prose; describe the row set and labels and let the skill generate the Markdown at runtime from the research files.
  </action>
  <acceptance_criteria>
    - Frontmatter contains exactly `name: ev-compare`, `allowed-tools: Read, Write, Glob`, and `disable-model-invocation: true`.
    - The `!`cat state.md`` backtick injection line is present; there is no `$ARGUMENTS` line.
    - A Glob step targets `projects/<active_project>/research/*.md` built from the resolved active_project (project-scoped, not hardcoded).
    - The table spec defines "WLTP range" and "Real-world range (mild)" and "Real-world range (cold)" as three separate rows, each with a methodology label.
    - Gap rendering is specified with explicit text such as `no FDM test` — never blank.
    - A `## Brief-Aware Verdict` section and condensed qualitative rows (FDM verdict, Styrker/Svagheder, ownership confidence) are specified.
    - The output is written to `projects/<active_project>/comparison.md`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^name: ev-compare$' .claude/skills/ev-compare/SKILL.md && grep -q '^disable-model-invocation: true$' .claude/skills/ev-compare/SKILL.md && grep -Eq '^allowed-tools:[[:space:]]*Read, Write, Glob' .claude/skills/ev-compare/SKILL.md && grep -q 'research/\*.md' .claude/skills/ev-compare/SKILL.md && grep -q 'comparison.md' .claude/skills/ev-compare/SKILL.md && grep -q 'WLTP range' .claude/skills/ev-compare/SKILL.md && grep -q 'Real-world range (mild)' .claude/skills/ev-compare/SKILL.md && grep -q 'Real-world range (cold)' .claude/skills/ev-compare/SKILL.md && ! grep -q '\$ARGUMENTS' .claude/skills/ev-compare/SKILL.md && echo PASS</automated>
  </verify>
  <done>The SKILL.md exists with correct write-side frontmatter, project-scoped Glob, separate labelled WLTP/real-world rows, explicit gap rendering, brief-aware verdict, and a comparison.md Write — and the structural gate prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Verify range-methodology + gap-rendering contract against car-template</name>
  <files>.claude/skills/ev-compare/SKILL.md</files>
  <read_first>
    - .claude/skills/ev-compare/SKILL.md (the file authored in Task 1)
    - car-template.md (lines 17-33 = the canonical 15 Specs rows the comparison must cover; lines 12-15 = the WLTP-vs-real-world separation note that D-16/COMP-02 enforce)
    - .planning/phases/03-search-and-compare/03-RESEARCH.md (Code Examples lines 514-524 = the required labelled-row header format; Pitfall 5 lines 470-476 = the cross-project read hazard)
  </read_first>
  <action>
    Confirm the authored skill honors the locked range-methodology and gap-rendering contract that COMP-02, D-16, and D-17 require, distinct from Task 1's frontmatter/structure gate. Assert the skill keeps WLTP range and BOTH real-world (mild + cold) ranges as separate rows with methodology labels (so the figures are unambiguous — Success Criterion 3), specifies explicit gap text (`no FDM test`) rather than blanks, marks best-in-class, and constructs the Glob path from `active_project` (guarding against the cross-project read in Pitfall 5). If any assertion fails, fix the SKILL.md to satisfy the contract. This is the goal-backward check that the comparison output will be unambiguous and decision-supporting before the manual golden run.
  </action>
  <acceptance_criteria>
    - All three range rows (WLTP, real-world mild, real-world cold) are present as separate labelled rows.
    - Explicit gap text (`no FDM test`) is present in the skill's gap-rendering instruction.
    - Best-in-class marking is specified.
    - The Glob/read path references `active_project` (project-scoped, not a bare global `research/` path).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'WLTP range' .claude/skills/ev-compare/SKILL.md && grep -q 'Real-world range (mild)' .claude/skills/ev-compare/SKILL.md && grep -q 'Real-world range (cold)' .claude/skills/ev-compare/SKILL.md && grep -q 'no FDM test' .claude/skills/ev-compare/SKILL.md && grep -Eqi 'best.in.class|best value|bold the' .claude/skills/ev-compare/SKILL.md && grep -q 'active_project' .claude/skills/ev-compare/SKILL.md && echo PASS</automated>
  </verify>
  <done>The skill provably keeps WLTP and FDM real-world ranges as separate labelled rows, renders gaps explicitly, marks best-in-class, and scopes reads to the active project — the gate prints PASS, satisfying COMP-02 / D-16 / D-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> skill | research/*.md content (which may embed text captured from earlier web fetches) is read into the comparison |
| skill -> filesystem | skill writes projects/<active>/comparison.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | research/*.md content echoed into comparison.md | mitigate | Render extracted values as table cells only; gaps rendered as fixed explicit strings; no execution of file content; the skill summarizes, it does not follow embedded instructions |
| T-03-05 | Information Disclosure | Glob across projects (PROJ-03) | mitigate | Glob path built solely from the resolved active_project; never a bare `research/` glob and never another project's path (Pitfall 5) |
| T-03-06 | Elevation of Privilege | accidental auto-invocation overwriting comparison.md | mitigate | `disable-model-invocation: true` in frontmatter — the skill runs only on explicit `/ev-compare` (Pitfall 4, D-18) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — only native Claude tools (Read, Write, Glob); no third-party packages (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `/ev-compare` in `projects/test-ev-detail-new` (3 researched cars) writes `comparison.md` with one column per car (COMP-01, COMP-03).
- The table shows WLTP range and FDM real-world (mild/cold) ranges as separate, methodology-labelled rows (COMP-02, D-16).
- Best-in-class is highlighted per row, a brief-aware verdict heads the file, and gaps (e.g. Renault tire 'unconfirmed', any 'no FDM test') render explicitly (D-14, D-17).
- The skill does not auto-trigger (disable-model-invocation: true) and reads only the active project's research files (D-18, PROJ-03).
</verification>

<success_criteria>
- COMP-01, COMP-02, COMP-03 satisfied: `/ev-compare` reads all active-project research/*.md, writes a one-column-per-car comparison.md, and labels range methodology per column.
- Both task gates print PASS.
</success_criteria>

<output>
Create `.planning/phases/03-search-and-compare/03-02-SUMMARY.md` when done.
</output>
