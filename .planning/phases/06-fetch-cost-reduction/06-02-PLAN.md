---
phase: 06-fetch-cost-reduction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/ev-detail/sites.md
  - .claude/skills/ev-detail/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Two RESEARCH open questions are resolved EARLY before the rest of Lever B is committed: (a) whether max_content_tokens is settable via SKILL.md prose or harness-level only; (b) whether ${CLAUDE_SKILL_DIR} backtick injection loads sites.md or a Read instruction is needed (RESEARCH Open Questions 1 & 3)"
    - "A shared sites.md supporting file carries per-site region selectors + URL patterns for the 4 known sites; adding/adjusting a site is a single localized edit (D-06, SC#5)"
    - "Each known-site fetch in ev-detail returns only the relevant content SECTION verbatim — never pre-parsed values — so the model still reads the figures (D-05, SC#2)"
    - "The region prompt for each site explicitly names every field the Phase 2 golden runs check, so no required field is silently dropped by trimming (SC#3, RESEARCH Pitfall 2)"
    - "A max_content_tokens backstop bounds every known-site fetch so an unexpected layout cannot blow the budget (D-05)"
    - "On an unknown site or a missing region, the fetch degrades to a bounded full fetch and never aborts (D-07, SC#4); the mandatory-source abort rule for ev-database.org is preserved"
    - "Trimming each page cuts per-page token weight (target ~80% on the 4 known sites), and in an agentic fork that cut multiplies across subsequent turns (D-09, D-10, SC#1 — measured in Plan 03)"
  artifacts:
    - path: ".claude/skills/ev-detail/sites.md"
      provides: "Lever B per-site region selectors + URL patterns + fallback rule (the single localized edit point for site changes)"
      contains: "ev-database.org"
      min_lines: 30
    - path: ".claude/skills/ev-detail/SKILL.md"
      provides: "Live-fetch steps (6-9) apply per-site region prompt + max_content_tokens backstop sourced from sites.md, with graceful degradation"
      contains: "sites.md"
  key_links:
    - from: ".claude/skills/ev-detail/SKILL.md"
      to: ".claude/skills/ev-detail/sites.md"
      via: "Read instruction (or ${CLAUDE_SKILL_DIR} backtick injection if probe confirms it works) sourcing region prompts before each WebFetch"
      pattern: "sites.md"
---

<objective>
Build Lever B: per-fetch section isolation. Create the shared `sites.md` supporting file (per-site region selectors + URL patterns for the four known sites: ev-database.org, fdm.dk, wheel-size.com, Bilbasen/manufacturer-DK), and modify `ev-detail/SKILL.md` Steps 6-9 so each known-site WebFetch applies the matching region prompt plus a `max_content_tokens` backstop — returning the section VERBATIM (never pre-parsed values), with graceful degradation to a bounded full fetch on unknown site / missing region.

Two RESEARCH open questions are front-loaded as the FIRST task so the rest of Lever B does not depend on an unverified assumption: (1) can `max_content_tokens` be applied via SKILL.md prose, or is it harness-level only; (2) does `${CLAUDE_SKILL_DIR}` backtick injection load `sites.md`, or is a Read instruction required.

Purpose: Drops per-page boilerplate for a large token cut (SC#1) while preserving HIGH robustness — the model still reads prose, so layout/wording drift between cars is absorbed (the "living tool" constraint). Lever B applies inside each fork; combined with Lever A's per-car isolation it removes both per-page weight and cross-car compounding (D-09, D-10).
Output: `.claude/skills/ev-detail/sites.md` (new) + modified `.claude/skills/ev-detail/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-fetch-cost-reduction/06-CONTEXT.md
@.planning/phases/06-fetch-cost-reduction/06-RESEARCH.md
@.planning/phases/06-fetch-cost-reduction/06-PATTERNS.md
@.claude/skills/ev-detail/SKILL.md
@car-template.md
@.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md
</context>

## Artifacts this plan produces

| Artifact | Type | Notes |
|----------|------|-------|
| `.claude/skills/ev-detail/sites.md` | NEW supporting file | Per-site region selectors + URL patterns + fallback rule; the single localized edit point (SC#5) |
| Region-selector entries | NEW content | ev-database.org (spec-table container), fdm.dk (article body), wheel-size.com (size block), Bilbasen/manufacturer-DK (price/listing region) |
| `ev-detail/SKILL.md` Steps 6-9 region-prompt preamble | MODIFIED steps | Read sites.md → apply region prompt → WebFetch with max_content_tokens backstop → graceful degradation |
| Probe finding (max_content_tokens settability; backtick-injection viability) | Decision recorded in SUMMARY | Determines whether sites.md loads via backtick injection or Read tool, and how the backstop is expressed |

<tasks>

<task type="auto">
  <name>Task 1: Resolve the two RESEARCH open questions (probe, front-loaded)</name>
  <read_first>
    - .planning/phases/06-fetch-cost-reduction/06-RESEARCH.md (Open Questions 1 & 3; Lever B "WebFetch dynamic filtering" + "Important caveat for Claude Code skills"; Assumptions A1, A3)
    - .claude/skills/ev-detail/SKILL.md (the existing fetch steps and the existing backtick injection at line 11)
    - .planning/phases/06-fetch-cost-reduction/06-PATTERNS.md ("sites.md Read instruction placement" — Option A backtick vs Option B Read)
  </read_first>
  <files>.planning/phases/06-fetch-cost-reduction/06-02-SUMMARY.md</files>
  <action>
    Run two quick live probes IN THIS Claude Code session and record the outcomes (these decide how Tasks 2-3 are written):

    Probe 1 — max_content_tokens settability (RESEARCH Open Question 1 / Assumption A1):
    WebFetch a single large page (e.g. an ev-database.org car page) with an explicit prose instruction to limit ingested content to roughly 2000 tokens (and, if the harness exposes it, the `max_content_tokens` parameter). Observe whether truncation/limiting visibly occurs. Conclude one of:
      (a) prose instruction alone limits ingestion → express the backstop as a prose "ingest at most ~N tokens" instruction per site in sites.md / SKILL.md; OR
      (b) it does not → record that the ceiling is harness-level and the region prompt is the primary lever, with the prose cap kept as a best-effort hint.

    Probe 2 — ${CLAUDE_SKILL_DIR} backtick injection of sites.md (RESEARCH Open Question 3 / Assumption A3):
    Determine whether a backtick-injection line of the form that cats `${CLAUDE_SKILL_DIR}/sites.md` would load the file at invocation time, versus requiring a Read-tool instruction at Step 6. (sites.md does not exist yet — test the mechanism with the existing `state.md` backtick form already in ev-detail line 11 as the reference, and/or a trivial file in the skill dir.) Conclude one of:
      (a) backtick injection works → Task 3 adds ONE injection line; OR
      (b) it does not → Task 3 uses a Read instruction in the Step 6-9 preamble.

    Record both findings (a/b decision + one-line evidence each) in the plan SUMMARY. Do NOT proceed to write sites.md region wording in a way that contradicts Probe 1's outcome.

    Do NOT git commit.
  </action>
  <verify>
    <human-check>SUMMARY records a definitive (a) or (b) outcome for BOTH probes with one line of evidence each.</human-check>
    <automated>MISSING — interactive live-fetch probe; no test runner exists. Outcome is recorded in 06-02-SUMMARY.md and gates how Tasks 2-3 express the backstop and the sites.md load mechanism.</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY states whether `max_content_tokens` is settable via SKILL.md prose (Probe 1: a or b) with evidence.
    - SUMMARY states whether `${CLAUDE_SKILL_DIR}` backtick injection loads a supporting file or a Read instruction is required (Probe 2: a or b) with evidence.
    - The chosen mechanisms are carried forward consistently into Tasks 2 and 3.
  </acceptance_criteria>
  <done>
    Both RESEARCH open questions are resolved with recorded evidence, so the rest of Lever B is built on verified behavior rather than assumption (RESEARCH Open Questions 1 & 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ev-detail/sites.md with per-site region selectors and fallback</name>
  <read_first>
    - .planning/phases/06-fetch-cost-reduction/06-PATTERNS.md ("sites.md (supporting config file)" — the structure template and field-coverage requirement)
    - .planning/phases/06-fetch-cost-reduction/06-RESEARCH.md ("Per-site region prompts — concrete wording" and the per-site field lists for SC#3)
    - .claude/skills/ev-detail/SKILL.md Step 6 (lines 124-145: the exact ev-database.org field table the region must cover)
    - .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md (the field-coverage baseline every region must still satisfy)
    - car-template.md (the per-car output contract — every field must remain obtainable from the isolated section)
    - 06-02-SUMMARY.md Probe 1 outcome (how to express the max_content_tokens backstop)
  </read_first>
  <files>.claude/skills/ev-detail/sites.md</files>
  <action>
    Create `.claude/skills/ev-detail/sites.md`. One section per known site, each with: a URL pattern, a Region prompt, and a max_content_tokens backstop value (Claude's Discretion per site; express it the way Probe 1 confirmed). Sections to create (the four known sites + fallback):

    - `## ev-database.org` — URL pattern `https://ev-database.org/[uk/]car/{ID}/{Make-Model-Variant}`. Region prompt: return ONLY the spec-table container — and EXPLICITLY name every required field group so none is silently dropped (RESEARCH Pitfall 2): range (WLTP, real-world mild, real-world cold), useable battery capacity, DC max / AC max / 10-80% charge time, 0-100 km/h (page shows 0-62 mph), boot/cargo, towing capacity, max power, AND the EV-dedicated-platform field (which may sit outside the spec table — call it out). Discard nav, header, footer, related cars, pricing, ads. This region MUST cover every row in SKILL.md Step 6's field table.
    - `## fdm.dk` — URL pattern `https://fdm.dk/tests/biltest/[slug]`. Region prompt: return ONLY the article body — measured range at 110 km/h 20°C, measured range at 0°C, FDM-measured DC charge rate, verdict, Styrker/Fordele, Svagheder/Ulemper, DKK prices (base + tested trim), publication date. Discard nav/header/footer/sidebar/related articles/consent banners.
    - `## wheel-size.com` — URL pattern `https://www.wheel-size.com/size/<make>/<model>/<year>/`. Region prompt: return ONLY the OEM tire-size block — front and rear OEM sizes (marked OE/OEM). Discard nav/ads/unrelated size listings.
    - `## Bilbasen Blog / manufacturer DK (purchase-type branch)` — note the three purchase-type sub-cases from RESEARCH ("Per-site region prompts", Bilbasen entry): used → Bilbasen Blog used-market price-range region (low/typical/high DKK + article date); new → manufacturer DK pricing section (tier 1 + tier 2 DKK); leasing → editorial monthly DKK/month range. Discard configurator/accessories/unrelated sections/comments.
    - `## Fallback (unknown site or missing region)` — instruction: if the URL matches no entry above, perform a bounded full fetch (max_content_tokens backstop only, no region prompt) and never abort (D-07, SC#4).

    Region prompts describe WHERE the content is, never WHAT values to extract — the section is returned verbatim and the model reads it (D-05; RESEARCH anti-pattern "Pre-parsing values in sites.md"). Keep it plain markdown (no JSON/YAML schema).

    Do NOT git commit.
  </action>
  <verify>
    <automated>test -f .claude/skills/ev-detail/sites.md && grep -q 'ev-database.org' .claude/skills/ev-detail/sites.md && grep -q 'fdm.dk' .claude/skills/ev-detail/sites.md && grep -q 'wheel-size.com' .claude/skills/ev-detail/sites.md && grep -qi 'bilbasen' .claude/skills/ev-detail/sites.md && grep -qi 'fallback' .claude/skills/ev-detail/sites.md && grep -qi 'platform' .claude/skills/ev-detail/sites.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/ev-detail/sites.md` exists with a section for each of: ev-database.org, fdm.dk, wheel-size.com, Bilbasen/manufacturer-DK, and a Fallback section.
    - Each known-site section has a URL pattern, a Region prompt, and a max_content_tokens backstop value.
    - The ev-database.org region prompt explicitly names the EV-dedicated-platform field (Pitfall 2) and covers every row of SKILL.md Step 6's field table.
    - The Fallback section states: bounded full fetch, never abort (SC#4).
    - No section pre-parses values into typed fields — regions are described as content sections only (D-05).
  </acceptance_criteria>
  <done>
    A shared `sites.md` carries per-site region selectors + URL patterns for the 4 known sites plus a never-abort fallback; each region names every field the golden runs check so trimming drops no required field. Adding/adjusting a site is a single localized edit here (D-06, SC#5).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire ev-detail Steps 6-9 to apply per-site region prompts + backstop with graceful degradation</name>
  <read_first>
    - .claude/skills/ev-detail/SKILL.md (the file being modified — Steps 6 lines 120-151, 7 lines 154-181, 8 lines 185-205, 9 lines 209-262; the existing backtick injection at line 11)
    - .claude/skills/ev-detail/sites.md (created in Task 2 — the region prompts to reference)
    - .planning/phases/06-fetch-cost-reduction/06-PATTERNS.md ("Steps 6-9 (modified, Lever B)" — the preamble shape; "sites.md Read instruction placement" Option A vs B)
    - .planning/phases/06-fetch-cost-reduction/06-RESEARCH.md (Pitfall 4: sites.md must be referenced or it is never loaded; the abort/degrade split)
    - 06-02-SUMMARY.md Probe 2 outcome (backtick injection vs Read instruction)
  </read_first>
  <files>.claude/skills/ev-detail/SKILL.md</files>
  <action>
    Modify `ev-detail/SKILL.md` so the four known-site fetch steps source their region prompt + backstop from `sites.md`. Per Probe 2 (Task 1):
    - If backtick injection works (Probe 2 = a): add ONE injection line near the existing `state.md` injection (line 11) that loads `sites.md` at invocation, e.g. catting `${CLAUDE_SKILL_DIR}/sites.md`. Then each step references "the region prompt for <site> from the injected sites.md".
    - If not (Probe 2 = b): add a Read instruction for `.claude/skills/ev-detail/sites.md` as the first line of the Step 6-9 preamble.

    For EACH of Step 6 (ev-database.org), Step 7 (fdm.dk fetch sub-step), Step 8 (wheel-size.com), and Step 9 (each purchase-type WebFetch), insert a preamble before the existing WebFetch instruction (06-PATTERNS.md "Modified Step 6 shape"):
    1. Apply the region prompt for this site from sites.md in the WebFetch instruction.
    2. Apply the max_content_tokens backstop from sites.md (expressed per Probe 1 outcome).
    3. Graceful degradation: if the region prompt yields no content or the layout changed, fall back to a bounded full fetch (backstop only). Then the EXISTING per-step abort/degrade rule still applies — preserve it exactly:
       - Step 6 (ev-database.org, mandatory): if the page cannot be fetched / car not found → ABORT, write NO file (D-03). Do NOT weaken this to a degrade.
       - Steps 7, 8, 9 (best-effort): on no result → existing gap-note behavior ("No FDM test found...", "Tire size: unconfirmed...", etc.), never abort (D-04/D-07).

    Do NOT change the field-extraction tables, the data-discipline rules, the variant-selection logic, or any step's output contract — only add the region-prompt/backstop/degradation preamble that controls HOW MUCH of each page is ingested. The returned section is read by the same downstream extraction that already exists (verbatim section in, same fields out — D-05/SC#2).

    Keep SKILL.md under 500 lines (CLAUDE.md). The per-site detail lives in sites.md; SKILL.md only references it (SC#5).

    Do NOT git commit.
  </action>
  <verify>
    <automated>grep -q 'sites.md' .claude/skills/ev-detail/SKILL.md && grep -ci 'region prompt' .claude/skills/ev-detail/SKILL.md | awk '{ if ($1+0 >= 1) print "PASS"; else print "FAIL" }' && test "$(wc -l < .claude/skills/ev-detail/SKILL.md)" -lt 500 && echo LINECHECK_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `ev-detail/SKILL.md` references `sites.md` (either a backtick injection line or a Read instruction, per Probe 2).
    - Steps 6, 7, 8, 9 each instruct applying the matching site's region prompt + max_content_tokens backstop before fetching.
    - Each step includes a graceful-degradation line (bounded full fetch on missing region) while PRESERVING the existing abort rule for ev-database.org (Step 6) and the existing gap-note degrade for Steps 7-9.
    - No field-extraction table, data-discipline rule, or output contract is altered.
    - SKILL.md remains under 500 lines.
  </acceptance_criteria>
  <done>
    The four known-site fetches in `ev-detail` now apply a per-site region prompt + backstop sourced from `sites.md`, returning the section verbatim with graceful degradation — cutting per-page weight (SC#1, measured in Plan 03) without dropping any field (SC#3) and without changing what the file contains (D-05, SC#2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public EV site → fork context | Fetched page content (now region-trimmed) crosses into the fork; read-only, untrusted |
| sites.md → SKILL.md | Region prompts loaded as instruction text; describe location only, not values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering (data integrity) | Region prompt silently drops a checked field | mitigate | Each region prompt EXPLICITLY names every required field group (esp. EV-platform, which may sit outside the spec table); validated against VALIDATION-CHECKLIST in Plan 03 (SC#3; RESEARCH Pitfall 2) |
| T-06-04 | Denial of service | Unexpected layout makes a region match the whole page (the very overflow this phase fixes) | mitigate | max_content_tokens backstop bounds every known-site fetch; fallback is a bounded full fetch, never an unbounded one (D-05, SC#4) |
| T-06-05 | Tampering | Prompt-injection in fetched page content steering research output | accept | Read-only fetch of known public EV sites; the model already interprets these pages today (no new surface vs current /ev-detail). Personal local tool; rate honestly as low |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Claude Code skills + native tools only (RESEARCH.md) |
</threat_model>

<verification>
- After Task 1: both probes resolved with recorded evidence.
- After Tasks 2-3: `/ev-detail "Volvo EX30"` single-car golden run still produces all VALIDATION-CHECKLIST fields and the fetched sections are visibly trimmed (full measurement deferred to Plan 03, SC#1/SC#3).
- `sites.md` and `ev-detail/SKILL.md` match the acceptance criteria; SKILL.md under 500 lines.
</verification>

<success_criteria>
- Per-site region prompts return sections verbatim, not parsed values (SC#2, D-05).
- Every checked field remains obtainable from the trimmed sections (SC#3) — region prompts name them explicitly.
- Unknown site / missing region degrades to a bounded full fetch, never aborts (SC#4); ev-database.org mandatory-abort preserved.
- Adding/adjusting a site is a single localized `sites.md` edit (SC#5, D-06).
- The two RESEARCH open questions are resolved before the approach is committed.
</success_criteria>

<output>
Create `.planning/phases/06-fetch-cost-reduction/06-02-SUMMARY.md` when done (include the Probe 1 & Probe 2 findings).
</output>
