---
phase: 06-fetch-cost-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/ev-research/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A new /ev-research skill exists that takes multiple quoted car names and spawns one isolated /ev-detail fork per car (D-02, SC#6)"
    - "The orchestrator collects back only each car's status line + result-file path — never page bodies or research-file content (D-02, SC#7)"
    - "The orchestrator SKILL.md explicitly prohibits reading the research files back after each fork returns (SC#7)"
    - "Any multi-car / validation run is intended to go through /ev-research rather than running cars sequentially in one context (D-03, root cause D-01)"
    - "The orchestrator is inline (no context: fork) so it can accumulate per-car status across iterations (D-04 sequencing premise)"
  artifacts:
    - path: ".claude/skills/ev-research/SKILL.md"
      provides: "Lever A batch orchestrator skill — fans out N isolated /ev-detail forks, collects status+path only"
      contains: "name: ev-research"
      min_lines: 40
  key_links:
    - from: ".claude/skills/ev-research/SKILL.md"
      to: ".claude/skills/ev-detail/SKILL.md"
      via: "prose instruction to invoke /ev-detail \"<car>\" per car (ev-detail's own context: fork provides isolation)"
      pattern: "ev-detail"
---

<objective>
Build Lever A: the new `/ev-research` batch-orchestrator skill (working name from CONTEXT.md D-02). It parses multiple quoted car names from `$ARGUMENTS`, dispatches one isolated `/ev-detail` fork per car, and collects back ONLY each car's status line + result-file path. The orchestrator context never holds page bodies or research-file content (SC#7), which structurally prevents the multi-car overflow that killed Phase 2 plan 02-03 (root cause D-01).

Purpose: This is the PRIMARY lever and lands first (D-04). Because each `/ev-detail` invocation already runs `context: fork, agent: Explore`, isolation per car is structural — the gap was the missing multi-car layer that honored it (D-01, D-03).
Output: `.claude/skills/ev-research/SKILL.md` (new skill directory + file).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-fetch-cost-reduction/06-CONTEXT.md
@.planning/phases/06-fetch-cost-reduction/06-RESEARCH.md
@.planning/phases/06-fetch-cost-reduction/06-PATTERNS.md
@.claude/skills/ev-new-project/SKILL.md
@.claude/skills/ev-switch-project/SKILL.md
@.claude/skills/ev-detail/SKILL.md
</context>

## Artifacts this plan produces

| Artifact | Type | Notes |
|----------|------|-------|
| `.claude/skills/ev-research/SKILL.md` | NEW skill | Lever A orchestrator; inline (NO `context: fork`); `disable-model-invocation: true` |
| `/ev-research` command surface | NEW command | `/ev-research "car1" "car2" ...` — multi-car batch research |

<tasks>

<task type="auto">
  <name>Task 1: Author the /ev-research orchestrator skill</name>
  <read_first>
    - .claude/skills/ev-new-project/SKILL.md (frontmatter + backtick state injection + `$ARGUMENTS` idiom — the primary analog per 06-PATTERNS.md)
    - .claude/skills/ev-switch-project/SKILL.md (no-argument handling pattern, lines 16-23)
    - .claude/skills/ev-detail/SKILL.md (Step 13 confirmation shape — what each fork returns; lines 321-329)
    - .planning/phases/06-fetch-cost-reduction/06-RESEARCH.md (Pattern 1, "Lever A — Multi-Fork Orchestrator: Detailed Mechanics", and the fork-boundary verbatim prose)
    - .planning/phases/06-fetch-cost-reduction/06-PATTERNS.md (Pattern Assignments for ev-research/SKILL.md — verbatim prose to include)
  </read_first>
  <files>.claude/skills/ev-research/SKILL.md</files>
  <action>
    Create `.claude/skills/ev-research/SKILL.md` (create the `ev-research` directory).

    Frontmatter (mirror the ev-new-project idiom, adapt fields per 06-PATTERNS.md):
    - `name: ev-research`
    - `description:` batch-orchestrator phrasing — researches one or more EV models by spawning one isolated /ev-detail fork per car; note it is suitable for roughly 2-6 cars per run (RESEARCH.md Pitfall 5).
    - `allowed-tools: Read, Bash(ls *)` — the orchestrator NEVER fetches pages and NEVER writes research files; it only reads state and lists projects.
    - `disable-model-invocation: true` — batch runner runs only on explicit invocation.
    - `argument-hint: ["car1" "car2" ...]`
    - Do NOT add `context: fork` — the orchestrator must be inline to accumulate per-car status across iterations (D-04 premise; RESEARCH.md anti-pattern "context: fork on the orchestrator").

    Body, in order:
    1. Backtick state injection — copy the exact form from ev-switch-project/SKILL.md lines 9-10 (`cat state.md ...`). The orchestrator needs the active project to confirm forks write to the right path.
    2. `$ARGUMENTS` reference line: `Cars to research: $ARGUMENTS`.
    3. No-argument handling: if `$ARGUMENTS` is empty, tell the user the expected invocation format (`/ev-research "car1" "car2" ...`) and stop — adapt ev-switch-project lines 16-23.
    4. Steps:
       - Step 1: Parse `$ARGUMENTS` as a shell-quoted list of car names (`$ARGUMENTS[0]`, `$ARGUMENTS[1]`, ... per RESEARCH.md Pattern 4). Build the working list.
       - Step 2: Confirm an active project exists (from injected state); if `active_project` is `none`, stop and direct the user to /ev-new-project or /ev-switch-project (mirror ev-detail Step 1).
       - Step 3: Dispatch — for each car in the list, instruct Claude to invoke `/ev-detail "<car>"` and wait for the fork to complete. Default to SEQUENTIAL foreground dispatch (Claude's Discretion D-04; RESEARCH.md Open Question 2 + Pitfall 3: sequential avoids rate limits and avoids background auto-deny of the overwrite prompt). Document in a short note that a user may opt into parallel dispatch but that background forks auto-deny the /ev-detail overwrite prompt, so existing research files should be deleted before a parallel re-run.
       - Step 4: After each fork returns, record ONLY the returned status line and the "File written:" path from the fork's Step 13 output.
       - Step 5: After all forks complete, print a final summary table: columns car | status (OK / FAILED) | path.
    5. Include the fork-boundary isolation prose VERBATIM (from RESEARCH.md / 06-PATTERNS.md): "After each /ev-detail invocation completes, record only the status line and file path from the fork's output. Do NOT use the Read tool on the research file. The research file is for Phase 3's /ev-compare skill, not for this orchestrator's context." (This is the SC#7 guarantee in prose.)

    Keep the file focused — it is a thin orchestrator, not a re-implementation of /ev-detail. Do NOT replicate any fetch logic from ev-detail here.

    Do NOT git commit — the orchestrator commits.
  </action>
  <verify>
    <automated>test -f .claude/skills/ev-research/SKILL.md && grep -q 'name: ev-research' .claude/skills/ev-research/SKILL.md && grep -q 'disable-model-invocation: true' .claude/skills/ev-research/SKILL.md && ! grep -q 'context: fork' .claude/skills/ev-research/SKILL.md && grep -q 'ev-detail' .claude/skills/ev-research/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/ev-research/SKILL.md` exists.
    - Frontmatter contains `name: ev-research`, `disable-model-invocation: true`, `allowed-tools: Read, Bash(ls *)`, `argument-hint`.
    - Frontmatter does NOT contain `context: fork` (orchestrator is inline).
    - Body contains the backtick `cat state.md` injection and a `Cars to research: $ARGUMENTS` line.
    - Body instructs invoking `/ev-detail "<car>"` per car and collecting only status + path.
    - Body contains the verbatim fork-boundary prose prohibiting Read on the research files (SC#7).
    - Body defaults to sequential dispatch and notes the parallel/background overwrite caveat (Pitfall 3).
  </acceptance_criteria>
  <done>
    A new `/ev-research` skill exists that, given multiple quoted car names, dispatches one isolated `/ev-detail` fork per car and reports only per-car status + file path — never page or file bodies (D-02, D-03, SC#6, SC#7). The orchestrator is inline, suitable for 2-6 cars, and explicitly forbids reading the research files back.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → /ev-research | User-supplied car names enter as `$ARGUMENTS`; treated as search strings only, never executed |
| /ev-research → /ev-detail fork | Orchestrator delegates fetching to isolated forks; only status+path crosses back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | Orchestrator context bloat | mitigate | Verbatim prose forbids Read on research files; only status+path crosses the fork boundary (SC#7) — the structural isolation this plan exists to provide |
| T-06-02 | Denial of service | Parallel fork fan-out hitting site rate limits | accept | Default is sequential foreground dispatch; parallel is opt-in and documented with its caveats (RESEARCH.md Open Question 2). Personal local tool, low volume |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — Claude Code skills + native tools only (RESEARCH.md: "no npm/pip packages") |
</threat_model>

<verification>
- `/ev-research "Volvo EX30"` (single-car smoke) dispatches one `/ev-detail` fork and reports status + path without ingesting the page or file body (deferred to Plan 03 live run; structural review here).
- Orchestrator SKILL.md frontmatter and body match the acceptance criteria above.
</verification>

<success_criteria>
- New `/ev-research` orchestrator skill exists and is structurally inline (no fork), delegating to `/ev-detail` per car.
- Only status + path cross back to the orchestrator (SC#7); research files are never read back.
- Supports the multi-car validation run that Plan 03 drives (D-03, SC#6).
</success_criteria>

<output>
Create `.planning/phases/06-fetch-cost-reduction/06-01-SUMMARY.md` when done.
</output>
