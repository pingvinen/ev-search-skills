---
phase: 06-fetch-cost-reduction
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A representative /ev-detail golden run consumes materially fewer fetch tokens than the Phase 2 baseline, measured per-site before/after (target ~80% on the 4 known sites) — D-08(a), SC#1"
    - "The 5 Phase 2 golden scenarios re-run THROUGH /ev-research reproduce every VALIDATION-CHECKLIST.md field — no field silently dropped by section isolation (D-08(b), SC#3)"
    - "The 5-car run through /ev-research completes without overflowing context and produces each car's research file (SC#6)"
    - "The orchestrator context holds only per-car status + file paths, never raw page content or research-file bodies, during the multi-car run (SC#7)"
    - "Returned sections are verbatim prose/table text, not pre-parsed JSON values (SC#2) — confirmed by inspecting the produced research files"
    - "Unknown site / missing region degraded to a bounded full fetch and never aborted during the run (SC#4)"
  artifacts:
    - path: ".planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md"
      provides: "Recorded per-site token before/after deltas (SC#1) + 5-car golden re-run sign-off against VALIDATION-CHECKLIST (SC#3, SC#6, SC#7)"
      min_lines: 30
  key_links:
    - from: ".planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md"
      to: ".planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md"
      via: "re-run reproduces every checked field through /ev-research"
      pattern: "VALIDATION-CHECKLIST"
---

<objective>
Validate the phase (D-08). Prove the two targets that the levers exist to hit:
(a) per-site before/after page-content token measurement for the ~80% claim (SC#1, D-08a); and
(b) re-run the 5 Phase 2 golden scenarios THROUGH the new `/ev-research` orchestrator, confirming no checked field is dropped (SC#3, against VALIDATION-CHECKLIST.md) and the run completes without overflow (SC#6) with the orchestrator holding only status + paths (SC#7).

Purpose: This is the phase gate. It closes the D-01 root cause (multi-car run no longer overflows because it goes through `/ev-research`, D-03) and certifies Lever B trims pages without losing fields. No clean Phase 2 baseline exists (02-03 overflowed before recording one), so the measurement is a deliberate before/after per D-08.
Output: `.planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fetch-cost-reduction/06-CONTEXT.md
@.planning/phases/06-fetch-cost-reduction/06-RESEARCH.md
@.planning/phases/06-fetch-cost-reduction/06-VALIDATION.md
@.planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md
@.planning/phases/02-detail-skill/02-VALIDATION.md
@.claude/skills/ev-research/SKILL.md
@.claude/skills/ev-detail/SKILL.md
@.claude/skills/ev-detail/sites.md
</context>

## Artifacts this plan produces

| Artifact | Type | Notes |
|----------|------|-------|
| `.planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md` | NEW results doc | Per-site token before/after table (SC#1) + 5-car golden re-run sign-off (SC#2-4, SC#6, SC#7) |

<tasks>

<task type="auto">
  <name>Task 1: Measure per-site token before/after (SC#1)</name>
  <read_first>
    - .planning/phases/06-fetch-cost-reduction/06-RESEARCH.md ("Token reduction estimate mechanics (D-08, SC#1)" and "Don't Hand-Roll" token-counting row)
    - .planning/phases/06-fetch-cost-reduction/06-VALIDATION.md (Manual-Only Verifications — the SC#1 measurement instructions)
    - .claude/skills/ev-detail/sites.md (the region prompts being measured)
  </read_first>
  <files>.planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md</files>
  <action>
    For each of the four known sites, capture a before/after page-content token (or character-count proxy) comparison and record it:
    - BEFORE = full-page fetch with no region prompt (the status-quo behavior, or the proxy: full page text vs the spec-table section character count per RESEARCH "Practical baseline proxy"). Capture BEFORE and AFTER from the SAME live page at measurement time — a fresh unfiltered fetch vs a region-filtered fetch of the current page. This makes the comparison self-contained and independent of Wave-1 execution order: it does NOT require a pre-modification snapshot of `/ev-detail`. If a usable Phase 2 golden-run output character count exists in the test-project research files, it may corroborate the BEFORE figure, but the live full-vs-filtered fetch is the source of record.
    - AFTER = region-trimmed fetch using the sites.md region prompt + backstop.
    Use a representative car (e.g. Volvo EX30) for ev-database.org / fdm.dk / wheel-size.com, and the matching purchase-type source for the Bilbasen/manufacturer-DK case. There is no API to measure mid-fork token usage — use the Claude Code context indicator delta or a character-count proxy (RESEARCH "Don't Hand-Roll"); approximate is sufficient for D-08.

    Record a per-site table (site | before tokens/chars | after tokens/chars | % reduction) in `06-03-VALIDATION-RESULTS.md`. State whether the ~80% target is met on the four known sites (SC#1). If a site falls short, note it and the likely remedy (tighter region wording or a lower backstop) per RESEARCH Assumption A4 — do not silently pass.

    Do NOT git commit.
  </action>
  <verify>
    <human-check>06-03-VALIDATION-RESULTS.md contains a per-site before/after token table with a % reduction per site and an explicit met/not-met verdict against the ~80% target (SC#1).</human-check>
    <automated>MISSING — token counts are observed from live fetch output, not assertable by a test runner (06-VALIDATION.md Manual-Only Verifications). Verified by the recorded per-site table in 06-03-VALIDATION-RESULTS.md.</automated>
  </verify>
  <acceptance_criteria>
    - A per-site before/after table exists for all four known sites with a % reduction each.
    - An explicit verdict states whether the ~80% target is met (SC#1); shortfalls are noted with remedy, not silently passed.
  </acceptance_criteria>
  <done>
    The ~80% per-page token-reduction claim is measured and recorded per site (SC#1, D-08a) — the levers are proven to cut fetch cost, not just asserted to.
  </done>
</task>

<task type="auto">
  <name>Task 2: 5-car golden re-run through /ev-research (SC#3, SC#6, SC#7)</name>
  <read_first>
    - .planning/phases/02-detail-skill/VALIDATION-CHECKLIST.md (the 5 golden scenarios + every checked field — the field-coverage baseline)
    - .planning/phases/02-detail-skill/02-VALIDATION.md (the 5 golden-run scenario definitions and test projects)
    - .claude/skills/ev-research/SKILL.md (the orchestrator under test)
    - .planning/phases/06-fetch-cost-reduction/06-RESEARCH.md (Pitfall 3: delete existing research files before a batch re-run so the overwrite prompt does not skip cars)
  </read_first>
  <files>.planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md</files>
  <action>
    Drive the 5 Phase 2 golden scenarios THROUGH `/ev-research` (not by running /ev-detail sequentially in one context — that is the D-01 failure this phase fixes, D-03). Per scenario, switch to the correct test project first (the scenarios span ev-detail-test-new, ev-detail-test-used, ev-detail-test-leasing). Before re-running, delete the existing research files for the cars being re-run so the /ev-detail overwrite guard does not skip them in a batch run (RESEARCH Pitfall 3).

    Then verify and record in `06-03-VALIDATION-RESULTS.md`:
    - SC#3: each produced research file reproduces every field checked in VALIDATION-CHECKLIST.md (no field dropped by section isolation). Record per-scenario pass/fail against the checklist field list.
    - SC#6: the 5-car run completes and writes every car's file without exhausting context (the run does not die at a SUMMARY step the way 02-03 did).
    - SC#7: confirm the orchestrator context held only per-car status lines + file paths — observe that context usage scaled with car count × (status+path size), not car count × page size. Confirm the orchestrator never read a research file back.
    - SC#2: spot-check that produced files contain verbatim prose/table figures (not JSON/structured-only output).
    - SC#4: if any scenario hit an unknown site / missing region, confirm it degraded to a bounded full fetch and still wrote the file (the leasing scenario's best-effort sourcing is a candidate).

    Record an overall phase-gate verdict. Best-effort gaps that already existed in Phase 2 (e.g. no EX30-specific leasing article) are noted, not failures (D-04).

    Do NOT git commit.
  </action>
  <verify>
    <human-check>06-03-VALIDATION-RESULTS.md records, per golden scenario, that every VALIDATION-CHECKLIST field is reproduced (SC#3), that the 5-car /ev-research run completed without overflow (SC#6), and that the orchestrator held only status+paths (SC#7).</human-check>
    <automated>MISSING — overflow and fork-boundary isolation are runtime context-window events only observable during a live multi-car run (06-VALIDATION.md Manual-Only Verifications). Verified by the recorded sign-off in 06-03-VALIDATION-RESULTS.md against VALIDATION-CHECKLIST.md.</automated>
  </verify>
  <acceptance_criteria>
    - All 5 golden scenarios run through `/ev-research` (not direct sequential /ev-detail), each in its correct test project.
    - Per-scenario record shows every VALIDATION-CHECKLIST field reproduced (SC#3).
    - The 5-car run completed and wrote all files without context exhaustion (SC#6).
    - Recorded confirmation that the orchestrator held only status + paths, never page/file bodies (SC#7).
    - Verbatim-section spot-check recorded (SC#2); any graceful-degradation case recorded (SC#4).
    - An overall phase-gate verdict is stated.
  </acceptance_criteria>
  <done>
    The 5 golden scenarios run through `/ev-research` reproduce every checked field, complete without overflow, and keep the orchestrator context to status + paths only — closing the D-01 root cause and certifying section isolation drops no field (D-08b, SC#3, SC#6, SC#7).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase gate — human verify token reduction + golden re-run</name>
  <action>Human checkpoint (no automated work): the user reviews 06-03-VALIDATION-RESULTS.md and confirms the phase gate per the how-to-verify steps below. Blocks until the user types "approved" or describes a shortfall.</action>
  <what-built>Lever A (`/ev-research` orchestrator) + Lever B (sites.md region prompts in `/ev-detail`), validated by a per-site token-delta measurement and a 5-car golden re-run through `/ev-research`, with results recorded in 06-03-VALIDATION-RESULTS.md.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/06-fetch-cost-reduction/06-03-VALIDATION-RESULTS.md`.
    2. Confirm the per-site token before/after table shows a meaningful reduction (target ~80%) on the four known sites (SC#1).
    3. Confirm the 5 golden scenarios each reproduce every VALIDATION-CHECKLIST field (SC#3) and the 5-car `/ev-research` run completed without context overflow (SC#6).
    4. Confirm the recorded note that the orchestrator held only status + file paths, never page/file bodies (SC#7).
    5. If any target is missed, describe what fell short so it can be addressed before the phase gate closes.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase gate, or describe what fell short.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation run → research files | Re-run overwrites test-project research files; confined to projects/ev-detail-test-* fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering (data integrity) | A dropped field passes unnoticed | mitigate | Re-run is checked field-by-field against VALIDATION-CHECKLIST.md (SC#3); no silent pass — shortfalls recorded |
| T-06-07 | Repudiation | Unverified "~80%" claim | mitigate | Per-site before/after measurement recorded with explicit met/not-met verdict (SC#1, D-08a) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — validation uses existing Phase 2 fixtures and native tools only (RESEARCH.md) |
</threat_model>

<verification>
- `06-03-VALIDATION-RESULTS.md` contains the per-site token-delta table (SC#1) and the 5-scenario field-coverage sign-off (SC#3) with overflow/isolation notes (SC#6, SC#7).
- Human checkpoint approves the phase gate.
</verification>

<success_criteria>
- SC#1: ~80% per-page token reduction measured per site (or shortfall noted with remedy).
- SC#3: every VALIDATION-CHECKLIST field reproduced through `/ev-research`.
- SC#6: 5-car run completes without overflow.
- SC#7: orchestrator holds only status + paths.
- Phase gate verdict recorded and human-approved.
</success_criteria>

<output>
Create `.planning/phases/06-fetch-cost-reduction/06-03-SUMMARY.md` when done (referencing 06-03-VALIDATION-RESULTS.md).
</output>
