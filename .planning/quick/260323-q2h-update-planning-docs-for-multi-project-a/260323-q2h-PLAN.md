---
phase: quick-260323-q2h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SRCH-01, DETL-02, COMP-01, COMP-03]
must_haves:
  truths:
    - "PROJECT.md describes multi-project architecture with projects/<name>/ structure"
    - "REQUIREMENTS.md has new requirements for /ev-new-project and /ev-switch-project skills"
    - "Existing requirements reference active project paths instead of hardcoded top-level paths"
    - "ROADMAP.md phase details reflect project-scoped paths"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Multi-project architecture description"
      contains: "projects/<name>"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Project management skill requirements"
      contains: "PROJ-01"
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase success criteria with project-scoped paths"
      contains: "active project"
  key_links: []
---

<objective>
Update PROJECT.md, REQUIREMENTS.md, and ROADMAP.md to reflect multi-project architecture.

Purpose: The repo will support multiple search projects (e.g., "family-ev", "commuter"), each in its own `projects/<name>/` folder with `search_criteria.md`, `research/`, and `comparison.md`. Two new skills (`/ev-new-project`, `/ev-switch-project`) manage projects. Existing skills operate within the active project. Planning docs must capture this before implementation begins.

Output: Three updated planning documents with multi-project requirements and paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md for multi-project architecture</name>
  <files>.planning/PROJECT.md</files>
  <action>
Update PROJECT.md with the following changes:

1. **"What This Is" section**: Update the first paragraph to mention that the suite supports multiple search projects, each in its own `projects/<name>/` folder.

2. **Active Requirements**: Add these new requirements at the end of the Active list:
   - `/ev-new-project "<name>"` scaffolds a project folder at `projects/<name>/` with `search_criteria.md` (from template), empty `research/` subfolder, and empty `comparison.md`
   - `/ev-switch-project "<name>"` sets the active project context so all other skills operate within that project
   - Each project is a self-contained silo — no cross-project comparison

3. **Update existing requirement references**:
   - Change "read search parameters from `car_search.md`" to "read search parameters from the active project's `search_criteria.md`"
   - Change "Per-car markdown files generated in `research/` directory" to "Per-car markdown files generated in the active project's `research/` directory"
   - Change "Comparison skill generates side-by-side table from existing per-car files" to reference "active project's per-car files"

4. **Constraints section**: Update the Input constraint from "`car_search.md`" to "active project's `search_criteria.md`". Update Output constraint from "`research/`" to "active project's `research/` subfolder".

5. **Context section**: Add a bullet: "Multiple search projects supported — each project gets its own folder under `projects/` with isolated search criteria, research files, and comparison output"

6. **Key Decisions table**: Add a row: "Multi-project architecture" | "Different searches (family car vs commuter) need separate criteria and results; a single car_search.md forces overwriting" | "-- Pending"

Keep all other content unchanged. Do NOT rename `car_search.md` references in the "What This Is" opening sentence — that is a general description. Only update the specific requirement bullets and constraints that describe file paths skills actually use.
  </action>
  <verify>
    <automated>grep -c "projects/" .planning/PROJECT.md | xargs test 3 -le</automated>
  </verify>
  <done>PROJECT.md describes multi-project architecture with projects/name/ structure, updated constraints, new requirements for project management skills, and a key decision entry.</done>
</task>

<task type="auto">
  <name>Task 2: Add project management requirements to REQUIREMENTS.md and update existing paths</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update REQUIREMENTS.md with these changes:

1. **Add new section "### Project Management"** after the "### Ownership Quality" section and before "## Out of Scope". Add these requirements:
   - `PROJ-01`: `/ev-new-project "<name>"` creates `projects/<name>/` with `search_criteria.md` (from template schema), empty `research/` subfolder, and placeholder `comparison.md`
   - `PROJ-02`: `/ev-switch-project "<name>"` sets active project context; subsequent skill invocations operate within that project's folder
   - `PROJ-03`: Each project is a self-contained silo — skills never read or compare across project boundaries
   - `PROJ-04`: Active project context persists across skill invocations within a session (no need to re-switch)

2. **Update existing requirement text** to reference active project paths:
   - `SRCH-01`: Change "structured criteria file" to "active project's `search_criteria.md`"
   - `DETL-02`: Change "`research/<make-model>.md`" to "active project's `research/<make-model>.md`"
   - `COMP-01`: Change "`research/*.md` files" to "active project's `research/*.md` files"
   - `COMP-03`: Change "`comparison.md`" to "active project's `comparison.md`"

3. **Update Traceability table**: Add rows for PROJ-01 through PROJ-04, all mapped to Phase 1, Status Pending. Update the Coverage count (was 24, now 28).

Do NOT change any other requirement text or mappings.
  </action>
  <verify>
    <automated>grep -c "PROJ-0" .planning/REQUIREMENTS.md | xargs test 4 -le</automated>
  </verify>
  <done>REQUIREMENTS.md has 4 new PROJ-* requirements in a Project Management section, existing path-dependent requirements updated to reference active project, traceability table updated with new requirements mapped to Phase 1.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md phases for project-scoped paths</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update ROADMAP.md with these changes:

1. **Phase 1 Requirements line**: Change `SRCH-01` to `SRCH-01, PROJ-01, PROJ-02, PROJ-03, PROJ-04`

2. **Phase 1 Success Criteria**: Add two new criteria:
   - Running `/ev-new-project "family-ev"` creates `projects/family-ev/` with `search_criteria.md`, `research/` subfolder, and `comparison.md`
   - Running `/ev-switch-project "family-ev"` sets active project context so that `/ev-detail`, `/ev-search`, and `/ev-compare` operate within `projects/family-ev/`
   - Update criterion 1 to reference active project's `search_criteria.md` instead of standalone `search_criteria.md`

3. **Phase 2 Success Criteria**: Update criterion 1 — change "`research/volvo-ex30.md`" to "active project's `research/volvo-ex30.md`"

4. **Phase 3 Success Criteria**:
   - Update criterion 1 — change "reads `search_criteria.md`" to "reads the active project's `search_criteria.md`"
   - Update criterion 2 — change "reads all `research/*.md` files and writes `comparison.md`" to "reads active project's `research/*.md` files and writes active project's `comparison.md`"

Keep all other content unchanged.
  </action>
  <verify>
    <automated>grep -c "active project" .planning/ROADMAP.md | xargs test 4 -le</automated>
  </verify>
  <done>ROADMAP.md Phase 1 includes PROJ-* requirements, all phase success criteria reference active project paths, Phase 1 has success criteria for /ev-new-project and /ev-switch-project.</done>
</task>

</tasks>

<verification>
All three planning documents consistently reference multi-project architecture:
- `grep "projects/" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows hits in all three files
- `grep "PROJ-0" .planning/REQUIREMENTS.md` shows 4 new requirements
- `grep "active project" .planning/ROADMAP.md` shows updated success criteria
- No remaining references to top-level `car_search.md` as the path skills use (general description references are fine)
</verification>

<success_criteria>
1. PROJECT.md describes the `projects/<name>/` folder structure and has requirements for `/ev-new-project` and `/ev-switch-project`
2. REQUIREMENTS.md has PROJ-01 through PROJ-04 in a new Project Management section, with traceability to Phase 1
3. Existing path-dependent requirements (SRCH-01, DETL-02, COMP-01, COMP-03) reference "active project" paths
4. ROADMAP.md Phase 1 includes PROJ-* requirements and success criteria for project scaffolding and switching
5. All phase success criteria use project-scoped paths instead of hardcoded top-level paths
</success_criteria>

<output>
After completion, create `.planning/quick/260323-q2h-update-planning-docs-for-multi-project-a/260323-q2h-SUMMARY.md`
</output>
